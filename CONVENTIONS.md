---
title: LLM Wiki 작성 규칙
type: reference
status: stable
tags: [convention, wiki]
created: 2026-04-27
updated: 2026-08-21
---

이 위키의 디렉토리 구조, 문서 작성 규칙, 메타데이터, 인용 원칙, 수정 금지 영역, 로그 양식을 정의함. AI 운영 규칙(역할, 사이클, 게이트, 검수)은 별도 [[AGENTS|에이전트 작업 지침]]에서 다룸. 본 문서는 AI/사람 누구든 위키에 글을 쓸 때 따르는 표준임. AGENTS.md가 본 문서를 `@CONVENTIONS.md`로 import 함.

# 1. 핵심 원칙

이 위키는 Andrej Karpathy의 **LLM Wiki** 패턴을 따르는 **프로젝트 지식 베이스**임. 원본을 질문마다 다시 읽는 RAG 대신, **수집(ingest) 시점에 지식을 위키로 컴파일**하고 시간이 지날수록 **복리(compounding)로 축적**함. LLM(Claude)이 마크다운 문서를 직접 작성/편집/연결하는 **위키 에디터** 역할을 함 ([[AGENTS#1. 핵심 원칙]]). 다만 외부 산출물(기획서/제안서 등)을 내는 과제 저장소라, 기본 패턴에 정본(`_locked`) 재대조와 엄격 인용([[#3.6 수치/목표 인용 원칙 (엄격)]], [[#3.7 외부 근거의 권위 계층]]), 다각도 검증([[AGENTS#2. 에이전트 작업 지침]])을 더한 변형임. 그 산출물 자체는 위키 문법을 못 쓰므로 `05_산출물/`로 층을 나눠 두고, **본 문서의 규칙은 위키층에만 적용함** ([[#2. 디렉토리 구조]]).

1. **Atomic Notes**: 하나의 파일에 하나의 주제. 파일명이 곧 제목.
2. **Wikilinks**: 문서 간 연결은 `[[문서명]]` 형식 (Obsidian 호환).
3. **점진적 축적**: 한 번에 완벽한 문서를 만들지 않고, 대화를 거듭하며 보강함.
4. **한국어 우선**: 문서는 한국어로 작성. 기술 용어는 영문 병기 가능. 영어 단독 작성 금지.
5. **엔티티 1급**: 고유명사(사람, 조직, 모델, 제품, 논문, 종목 등)는 `type: entity` 페이지로 분리해 상호연결의 허브로 삼음 ([[#3.2 프론트매터 (YAML)]]).
6. **링크 하한선**: 모든 페이지는 관련 페이지와 `[[ ]]`로 연결함 (고아 방지, 복리 그래프 강제). 하한 개수와 예외는 [[#3.5 Wikilink 규칙]]이 정함 (같은 규칙을 두 곳에 적지 않음, 2026-08-21).
7. **충돌 시 사실 우선**: 규칙끼리 충돌하면 어느 쪽을 따를지 사실 > 구조 > 형식 순으로 정함. `원문 미명시`는 표와 목록의 칸을 채우는 정당한 값이며, 칸이나 병렬 구조를 채우려고 사실을 지어내지 않음.

# 2. 디렉토리 구조

```
/                     # vault root
├── AGENTS.md         # 에이전트 작업 지침 (AI 운영 규칙)
├── CONVENTIONS.md    # 이 문서: 위키 작성 규칙
├── CLAUDE.md         # Claude Code 자동 로드 진입점 (AGENTS.md import 스텁)
├── 00_Index/         # 목차, 지도 문서 (MOC: Map of Content). 참조 순서 면제
├── 01_References/    # 원자료: 정본, 변환본, 외부 자료 요약과 인용
│   ├── _locked/      # 정본 (RFP, 확정본). 에이전트 read-only
│   ├── _sources/     # 변환 원본 (DOCX, HWPX 등). 에이전트 read-only
│   ├── _figures/     # 변환 추출 이미지 (영구 자산). 변환 파이프라인만 생성/덮어쓰기
│   ├── _reviews/     # 변환 검수 큐 사이드카 (일시 자산, 검수 후 삭제). 변환 파이프라인만 생성
│   └── converted/    # 변환 본문 .md (1차 탐색 대상). 에이전트 수정 가능
│       ├── 연구개발계획서/  # 제안서(HWPX) 변환본. 고정 집합
│       └── 남사당놀이/      # 종목 소재 전사본(스캔). 종목별 증가
├── 02_HowTo/         # 위키 관리 가이드 (작성, 검수, 수집, 정비)
├── 03_Concepts/      # 개념 정리 (정의, 원리, 이론)
├── 04_Projects/      # 위키층: 에이전트가 위키 방식으로 쓰는 지식 (설계 노트, 회의록, 자산)
│   └── _archive/     # 폐기 문서 (status: deprecated). 이력 보존용 read-only
├── 05_산출물/         # 산출물층: 사용자가 직접 쓰는 원고 (콘텐츠 기획서, 발표자료). 위키 규칙 미적용
├── 06_클라이언트 데이터/ # 클라 개발자가 앱에 로드하는 프로덕션 데이터 (JSON 등). 문서 삽화/마크다운과 분리
│   ├── 공통/          # 5종 시범콘텐츠 공유 데이터 (예: localization.json)
│   └── {콘텐츠}/       # 콘텐츠별 데이터 (예: 유람기/character_name_pool.json)
├── 07_작업노트/       # 에이전트 작업 노트 (작성/정비 TODO, 표준 정합 잔여). 사용자 결과물 아님
├── 08_제작 가이드/     # 위키 내용으로 외부 리소스를 만드는 지침 (인포그래픽 제작, 클라 데이터 번역)
├── 90_Temp/          # 승격 전 대기소 (외부 초안, 검토안). 승격하며 비움
├── 99_Logs/          # 작업 로그, 학습 일지, 실험 기록
├── scripts/          # 자동화 스크립트 (변환 파이프라인 등)
├── prototype/        # 브라우저에서 돌려 보는 프로토타입. 이 폴더만 압축해 공유함
│   └── assets/       # 프로토타입 전용 그림 (자족성 위해 여기 둠, 아래 예외 참조)
├── assets/           # 이미지, 첨부파일
└── export/           # DOCX 제출본 변환 산출물 (git 미추적, 재생성 가능)
```

**폴더 번호는 참조 순서임** (2026-08-21 개편): 번호 N 폴더의 문서는 자기보다 앞 번호 폴더의 문서만 근거로 삼고, 뒷 폴더의 문서를 참고하지 않음. 지식은 원자료(01) → 규칙(02) → 개념(03) → 설계(04) → 산출물(05) → 클라이언트 데이터(06) → 작업노트(07) → 제작 가이드(08) 순으로 상류에서 하류로 흐름. 90번대는 번호 폴더(01~08) 뒤에 오는 메타 층임: 90 대기소는 승격 전 초안이라 모든 폴더를 참고할 수 있고, 99 로그는 전체를 가리키는 이력임. 뒷 폴더를 가리키는 전방 wikilink는 `scripts/wiki_lint.mjs`가 경고함: 경고일 뿐 커밋을 막지 않으며, 해당 문서를 고칠 때 함께 정리함.

- **순서 밖 (검사 제외)**: `00_Index/`(색인은 전체를 가리킴), 무번호 소문자 폴더(assets, export, prototype, scripts: 지식이 아닌 인프라/자원층), 루트 규칙 문서(AGENTS, CONVENTIONS, CLAUDE, README)
- **개편 경위**: 2026-08-19에 `05_산출물/` 신설로 이하 폴더 번호를 한 칸씩 밀었고, 2026-08-21에 참조 순서 원칙을 도입하며 References와 Concepts를 맞바꿨고(원자료가 개념의 상류), 로그를 08에서 99로, 대기소를 99에서 90으로 옮김

변환 산출 바이너리 파일은 `assets/` 또는 `01_References/_figures/` 외 위치에 두지 않음. 예외는 `prototype/assets/` 하나임 (아래 참조).

**프로토타입 자족성**: `prototype/`은 팀원에게 **이 폴더만 압축해 보내면 바로 열리는** 상태를 유지함. 그래서 프로토타입이 쓰는 그림은 `assets/`가 아니라 `prototype/assets/`에 둠 (2026-08-11 사용자 결정). `06_클라이언트 데이터/`가 "개발자는 이 폴더 하나만 받으면 됨"으로 분리된 것과 같은 이유임.

- **자족성 규칙**: 폴더 밖을 가리키는 참조를 만들지 않음. CDN, 웹폰트, 외부 스크립트도 쓰지 않고 시스템 폰트만 씀
- **공유용 그림 규격**: 알파가 없는 사진은 JPEG(품질 92)로, 알파가 필요한 그림은 PNG로 둠. 화면 표시 크기의 두 배를 넘으면 줄임. 사진을 PNG로 두면 용량이 열 배 가까이 커짐 (미리보기 3장 실측 7.6MB → 0.9MB)
- **원본 보존**: 손실 압축을 적용하기 전에 원본을 먼저 커밋해 이력에 남김
- **동봉 안내문**: `prototype/README.md`는 위키 밖에서 읽히므로 프론트매터와 헤딩 넘버링 규칙을 따르지 않음 (`wiki_number.mjs`의 `NO_NUMBER` 예외)

**시범콘텐츠 폴더 넘버링** (2026-08-19 사용자 결정): `04_Projects/시범콘텐츠/`와 `05_산출물/시범콘텐츠/`가 같은 규칙을 씀. 공통이 맨 앞에 오고, 그 뒤로 종목 순서대로 늘어서면서 콘텐츠마다 폴더 하나를 가짐.

- **형식**: `{종목번호}_{종목명} - {콘텐츠명}`. 공통 자료는 `00_공통`
- **종목번호**: [[남사당놀이]] 여섯 종목의 순서를 그대로 씀. 풍물 01, 버나 02, 살판 03, 어름 04, 덧뵈기 05, 덜미 06. 종목이 늘거나 줄지 않으므로 번호가 흔들리지 않음
- **번호는 겹쳐도 됨**: 한 종목에 콘텐츠가 둘이면 `05_덧뵈기 - 나만의 탈춤`과 `05_덧뵈기 - 양반 놀리기 4컷`처럼 앞자리를 나눠 씀. 번호는 정렬 순서를 정할 뿐 폴더를 식별하는 값이 아님
- **한 겹으로 둠**: 종목 폴더 아래 콘텐츠 폴더를 두는 중첩을 쓰지 않음. 깊이가 일정해야 문서에서 `assets/`로 가는 상대경로가 폴더마다 달라지지 않음
- **종목 공용 자료**: 한 종목의 여러 콘텐츠가 함께 쓰는 자료가 생기면 `05_덧뵈기 - 공용`처럼 폴더를 따로 둠

**converted 하위 폴더 분리**: `converted/`는 성격이 다른 두 종류를 하위 폴더로 나눠 둠. 사업 제안서 변환본은 `연구개발계획서/`(고정 5개), 무형유산 종목 소재 전사본은 `남사당놀이/`(종목별 증가). 변환 파이프라인(`/import-doc`)은 신규 변환을 `converted/` 루트로 출력하므로, 소재 전사본은 변환 후 해당 하위 폴더로 사람이 옮김(어떤 문서가 들어올지 미정이라 자동 라우팅 대신 수동 이동 규약). wikilink는 파일명으로 해석되어 하위 폴더 이동이 링크를 깨지 않음.

**클라이언트 데이터 분리**: `06_클라이언트 데이터/`는 클라 개발자가 앱에 직접 로드하는 프로덕션 데이터(로컬라이제이션, 이름 풀, 설정 등)만 둠. 소비자가 위키 독자가 아니라 개발자라, 기획서 삽화(`assets/`)나 마크다운 문서(`04_Projects/`, `05_산출물/`)와 섞지 않음. 개발자는 이 폴더 하나만 받으면 됨. 각 데이터 파일의 용도/스키마/검수 상태는 폴더 루트 `README.md`가 안내함. 번역 표기와 낱말 규칙, 검수 절차는 [[클라이언트 데이터 번역 규칙]]이 소유하고 이 폴더의 파일을 상대경로로 가리킴(데이터는 개발자, 규칙은 사람과 검수로 물리 분리하되 참조로 연결). 이 폴더의 JSON은 기계 대상이라 가독성/낯선 독자 검토를 면제하고, 값의 정합(번역 정확성, 용어 일관성, 스키마)만 검수함.

**위키층과 산출물층 분리** (2026-08-19 사용자 결정): 결과물을 두 층으로 나눔. 한 폴더에 섞여 있을 때 위키 규칙이 기획서에 맞춰 휘었기 때문임.

- **`04_Projects/` 위키층**: 에이전트가 **위키 방식으로** 씀. 본 문서의 규칙을 전부 적용함. 링크를 조밀하게 걸고 원자 단위로 쪼갬. 설계 노트, 회의록, 콘텐츠 자산이 여기 있음
- **`05_산출물/` 산출물층**: 사용자가 **직접** 씀. 외부로 나가는 원고라 wikilink와 임베드를 못 쓰므로 **본 문서의 규칙을 적용하지 않음**. 콘텐츠 기획서 6종과 킥오프 발표자료가 여기 있음. 사양은 설계 정본이라 위키층에 둠. 에이전트 개입 절차는 [[AGENTS#2.9 산출물층 개입 절차 (05_산출물)]]가 정함
- **`07_작업노트/` 운영층**: 에이전트가 작성과 정비에 쓰는 지침과 TODO(표준 정합 잔여, 가독성 정비 항목 등). 결과물에 섞지 않고 콘텐츠별로 분리함(`type: agentnote`)

일반 작성 표준은 [[CONVENTIONS]], [[AGENTS]], `02_HowTo/`에 둠. 콘텐츠별 설계 결정과 사용자가 결정할 미결은 결과물에 그대로 둠(사용자가 보는 내용임).

# 3. 문서 작성 규칙

## 3.1 파일명

- 파일명에 언더스코어(`_`)를 쓰지 않음: Obsidian이 공백을 지원하므로 낱말 사이는 공백으로 둠
- 한국어 파일명 사용 가능 (예: `트랜스포머.md`)
- 영문 약어가 보편적인 경우 영문 사용 (예: `LLM.md`, `RAG.md`)

## 3.2 프론트매터 (YAML)

모든 문서 상단에 다음 메타데이터를 포함함. **`title` 필드가 문서의 유일한 제목원**: 본문에 H1 제목 재기재 금지([[#4. 헤딩 / 넘버링]] 참조).

```yaml
---
title: 문서 제목
type: entity | concept | overview | howto | reference | project | index | answer | log | agentnote
status: stub | draft | stable | deprecated
tags: [태그1, 태그2]
sources: [출처페이지명, ...]
created: YYYY-MM-DD
updated: YYYY-MM-DD
---
```

**필드 설명**

- **type**: 문서의 종류. `title`이나 폴더와 별개로 문서 성격을 분류함. `entity`는 주로 `03_Concepts/`에 두고 type으로 구분. 값은 아래 표 참조
- **status**: 성숙도. `stub`(껍데기)에서 `draft`(초안)을 거쳐 `stable`(안정)으로 올림. 오래 `stub`이나 `draft`에 머물면 유지보수 점검 대상 ([[AGENTS#2.8 위키 운영 워크플로 (수집/질의/유지)]] 3). `deprecated`는 폐기된 문서로, 더는 참조 대상이 아니며 이력 보존용으로만 남김 (주로 `_archive/`에 둠). 폐기 사유와 정본 안내는 본문 상단 `> [폐기]` 블록에 적음
- **sources** (선택): 이 페이지가 인용한 정본이나 변환본 페이지명을 나열함 (예: `sources: [1. 연구개발의 필요성]`. 대괄호는 YAML 리스트 표기이고 wikilink가 아님). 본문 출처 wikilink([[#3.6 수치/목표 인용 원칙 (엄격)]])를 frontmatter에서 한눈에 보게 하는 보조 색인임. 별도 source-id 레지스트리를 두지 않고 기존 wikilink 인용 모델을 따름
- **tags**: 자유 주제 태그. 문서 종류의 정본은 type이고, tags는 보조 분류임

쓸 수 있는 `type` 값:

| 값 | 의미 |
|---|---|
| `entity` | 고유명사 페이지(사람, 조직, 모델, 제품, 논문, 종목). 위키 상호연결의 중추 ([[#1. 핵심 원칙]] 5) |
| `concept` | 개념, 원리, 분류 |
| `overview` | 한 주제를 서술형으로 종합한 페이지 ([[위키 운영 워크플로#4. Overview(종합) 페이지]]) |
| `howto` | 가이드나 레시피 |
| `reference` | 외부 자료의 요약과 인용 |
| `project` | 프로젝트 노트 |
| `index` | MOC 등 색인 |
| `answer` | 질의에서 위키로 환원된 페이지 ([[AGENTS#2.8 위키 운영 워크플로 (수집/질의/유지)]] 2) |
| `log` | 로그 |
| `agentnote` | 에이전트 작업 노트 (작성/정비 TODO, 표준 정합 잔여 등). `07_작업노트/`에 두며 사용자 결과물 아님 |

## 3.3 본문 구조

H1 제목 라인은 두지 않음: frontmatter `title`이 단일 제목원. 본문은 한 줄 요약(TL;DR) 인용블록부터 시작하고, 섹션은 H1(`#`)부터 사용함.

```markdown
> 한 줄 요약 (TL;DR)

# 1. 핵심 내용

- ...

# 2. 세부 설명

- ...

# 3. 관련 문서

- [[관련문서1]]
- [[관련문서2]]

# 4. 참고

- 출처, URL 등
```

## 3.4 문체

### 3.4.1 기본 문체

- **두괄식**: 결론, 핵심을 먼저 쓰고, 부연은 뒤에 배치
- **개조식**: 불릿 포인트 중심으로 간결하게 서술
- **음슴체**: "~이다", "~한다" 대신 "~임", "~함" 체를 사용
- 한 문장은 짧게, 불필요한 수식어 제거
- 표, 리스트를 적극 활용하여 가독성 확보
- **가운뎃점(`·`) 사용 금지**: 항목 나열은 쉼표(`,`) 또는 슬래시(`/`)로 대체
- **em dash(`—`, U+2014) 사용 금지**: 부연/삽입은 쉼표(`,`), 콜론(`:`), 괄호로 대체하거나 문장을 나눔

### 3.4.2 쉽게 쓰기

어려운 말보다 쉬운 말. 읽는 이(미래의 나, 위키를 다시 읽을 사용자)가 한 번에 이해하게 씀.

- **쉬운 말 우선**: 어려운 한자어/외래어 대신 친숙한 우리말. 예: 제고함 → 높임, 소정의 → 정해진, 동 사업 → 이 사업, 타 기관 → 다른 기관. 단 기술 표준 용어(transformer, embedding 등)는 영문 병기 허용([[#1. 핵심 원칙]] 4)
- **약어 첫 등장 시 본디 용어 병기**: 줄임말 남용 금지. 예: 검색 증강 생성(RAG), 사업 모델(BM)
- **번역 투, 과도한 피동/사동 절제**: 예: 변경됨에 의해 → 변경됨, 환기를 시켜야 → 환기를 해야
- **중복/중의/군더더기 제거**: 같은 뜻 반복 금지(사전 동의가 반드시 필요 → 사전 동의가 필요), 한 가지 뜻으로만 읽히게 씀
- **설계 은어 '축' 금지**: 분류 기준이나 채점 요소를 '축'이라 부르지 않음. 가리키는 실체의 이름으로 씀. 예: 분류 축 → 분류, 두 축의 가중치 → 두 점수(또는 두 기준)의 가중치, 난이도 축 → 난이도 단계. '직교하는' 같은 수학 은어도 '별개 기준' 등으로 풀어 씀 (2026-08-05 사용자 지적, 저장소 전수 치환)

### 3.4.3 읽기 쉬운 개조식

개조식은 스캔하기 쉬우라고 쓰는 형식임. 압축 도구로 쓰면 거꾸로 읽기 어려워짐. 음슴체/개조식을 지키면서 사람이 한 번에 읽히게 씀.

- **한 불릿 한 생각**: 한 불릿에 개념 하나. 셋 이상 묶이면 sub-bullet로 분리
- **슬래시 절제**: `/`는 진짜 대안(SMS/이메일)이나 동의어에만. 관계 있는 항목은 "와", "및" 또는 줄바꿈으로 풂. "A/B/C 시 D 또는 E" 식 압축 금지
- **목록/표 앞 길잡이 한 줄**: 표나 불릿 묶음 앞에 무엇을 담는지 한 줄로 안내. 표 앞 길잡이는 표의 열 머리말을 복창하지 않음 (열 이름만 다시 나열하면 정보가 0). 표가 무엇을 담는지로 방향을 줌 (예: "분류, 값, 보관과 파기." → "세션이 담는 데이터를 분류, 값, 보관과 파기 정책으로 정리함"). 섹션 헤더 바로 밑 인트로 문장도 마찬가지로 헤딩을 풀어 옮기기만 하면 정보가 0이라 두지 않음. 헤더 뒤에 표나 불릿 묶음이 바로 오지 않으면 억지 인트로 없이 하위 절로 바로 감 (예: "# 3. 공용 시스템" 밑에 "여러 sub-step에 걸쳐 쓰이는 시스템과 횡단 정책임" 같은 헤딩 복창 줄을 두지 않음)
- **음슴체 완결**: 전보문식 압축("미검출/저신뢰/지연 시 보류")을 조사/연결을 살린 짧은 음슴체 문장으로. 줄글로 흐르는 문장은 명사로 끊지 말고 "임/함" 서술어로 닫음 (예: "최소 보관 원칙." → "최소 보관 원칙을 적용함"). 다만 라벨:값 불릿("역할: ...")이나 표 셀의 명사 종결은 정상 개조식이라 예외
- **두괄식 + 자족성**: 불릿이 단독으로 이해되게. 맥락 없는 라벨로 갈음 금지 ([[반복 결함 카탈로그]] CONTENT-002)
- **시각적 호흡**: 빽빽한 한 줄보다 짧은 두세 줄. 제목과 본문 묶음이 생기면 헤더로 ([[반복 결함 카탈로그]] STRUCT-007)

## 3.5 Wikilink 규칙

- **링크 하한선**: 모든 페이지는 관련 페이지를 최소 2개 `[[ ]]`로 연결함 (고아 방지, [[#1. 핵심 원칙]] 6). 순수 색인(MOC), 로그, 작업 노트(agentnote)는 예외. 링크 2개 미만은 유지보수 점검 대상 ([[AGENTS#2.8 위키 운영 워크플로 (수집/질의/유지)]] 3)
- 새 개념이 등장하면 `[[개념명]]`으로 링크를 검
- 링크 대상 문서가 아직 없어도 괜찮음 (나중에 생성)
- 별칭이 필요하면 `[[파일명|표시텍스트]]` 형식 사용
- 섹션 anchor는 `[[파일명#헤딩텍스트]]` 형식으로, 번호를 포함한 헤딩 텍스트 전체를 그대로 씀 (예: `[[CONVENTIONS#4.2 헤딩 넘버링]]`). 자기 문서의 절은 `[[#4.2 헤딩 넘버링]]`으로 씀
- 임베드(`![[ ]]`) 대상과 이미지 경로가 실제로 있는지는 `scripts/wiki_lint.mjs`가 기계로 검사함

**절 참조는 anchor로만 (§ 표기 폐지)**

문서의 절을 가리킬 때 `§4.가` 같은 번호 표기를 쓰지 않음. 다른 문서든 자기 문서든 wikilink anchor 하나로 통일함.

| 가리키는 대상 | 표기 |
|---|---|
| 다른 문서의 절 | `[[CONVENTIONS#4.2 헤딩 넘버링]]` |
| 자기 문서의 절 | `[[#4.2 헤딩 넘버링]]` |

- **왜 폐지했나**: § 표기는 본문 자연어에 녹아 있어 lint가 stale을 검출하지 못함. 절 번호가 바뀌어도 아무도 모르게 깨진 채로 남음(2026-07-14 이관 때 이미 깨져 있던 참조 15건이 드러남). anchor는 `wiki_lint`가 헤딩 텍스트와 대조해 검증하고, 재번호할 때 `wiki_number`가 자동으로 따라 고침
- **범위 참조**: 여러 절을 한꺼번에 가리켜야 하면 anchor를 나열하거나 말로 풀어 씀. `§1-4` 같은 표기는 헤딩 하나를 가리키지 않아 anchor로 옮길 수 없음
- **§ 를 남겨두는 곳**: `99_Logs/log.md`, `04_Projects/_archive/`, `02_HowTo/반복 결함 카탈로그.md`. 모두 당시 문서 상태를 인용한 이력이라 그대로 보존함

## 3.6 수치/목표 인용 원칙 (엄격)

문서에 등장하는 모든 **구체적 수치**와 **목표**는 반드시 `01_References/` 하위 원문을 직접 참조하여 작성함. 임의 추정/창작 금지.

**대상 항목**

- 정량 지표: 예산, 인원, 기간, 페이지 수, 비율(%), 점수, KPI, TRL 등
- 목표 진술: 사업 목표, 성과 목표, 정량/정성 목표, 핵심 성과지표
- 일정/기한: 착수일, 종료일, 단계별 마일스톤, 마감 일자
- 조직 정보: 컨소시엄 구성, 역할 분담, 기관별 담당 비율
- 산출물 명세: 결과물 수량, 규격, 인도 조건
- 원전 서사 사실: 전사본이 전하는 사건의 진행 순서, 결말과 승패, 인물의 행위 (2026-08-21 추가)

**작성 절차**

1. 수치/목표를 적기 전에 `01_References/` 하위 원문(특히 `_locked/RFP.md`, `_locked/컨소시엄 별 역할.md`)을 먼저 확인
2. 문서 내 인용 시 출처를 wikilink로 명시: 예: `[[RFP]] §3.2`, `[[컨소시엄 별 역할#수행기관]]`
3. 원문에 해당 정보가 없으면 **추정 금지**: `원문 미명시` 또는 `확인 필요`로 표기
4. 원문 간 수치가 충돌하면 양쪽을 모두 표기하고 사용자에게 확인 요청
5. 외부 자료(웹, 일반론)에서 가져온 수치는 과제 고유 수치와 명확히 구분 표기 (출처 URL 포함)
6. 전사본의 서사 사실을 다루는 표와 목록에는 `근거` 열이나 필드를 두고, 행 범위(예: `618~622행`) 또는 `원문 미명시`를 적음. 적은 행 번호가 전사본 범위 안인지는 `scripts/quote_check.mjs`가 검사하되, 요약이 원문에 충실한지는 기계가 못 보므로 작성자 재대조와 외부 검토 몫으로 남음 (2026-08-21 신설)

**금지 사항**

- 그럴듯해 보인다는 이유로 임의 수치 제시 금지
- 시장 평균치/업계 통상치를 과제 고유 수치인 양 기재 금지
- 출처 없이 "약 N억 원", "N개월", "N명 규모" 같은 어림값 작성 금지
- 원문 수치를 단위 변환/반올림하여 적을 때도 원문 표기를 함께 병기

## 3.7 외부 근거의 권위 계층

문서의 주장과 검수 토론에서 "외부 근거"의 권위를 5단계로 구분함. L1이 가장 강력, L5가 가장 약함. 충돌 시 상위 등급이 우선.

| 등급 | 출처 | 인정 범위 |
|---|---|---|
| L1 정본 | `01_References/_locked/`, `01_References/_sources/` 원본 | 수치/일정/정의의 최종 근거. L1과 충돌 시 본문이 틀린 것 |
| L2 변환본 | `01_References/converted/` | L1에서 파생. L1 재대조 가능할 때만 인용. L1과 어긋나면 L1 우선 |
| L3 내부 결정 | `04_Projects/` 내 사용자 확정 결정사항 | 본 저장소 내부에서만 유효. 외부 노출 시 정본 정합성 재확인 |
| L4 외부 자료 | 웹, 일반 문헌, 통상치 | 인용 시 URL 명시 + 과제 고유 수치와 명확히 구분. 정본 대체 불가 |
| L5 추론 | 에이전트 추론 | "추정", "확인 필요" 표기 의무. L1~L4 근거 없이 본문 단정 금지 |

**적용 규칙**

- 검수 발견에서 "근거 약함" 플래그가 나오면 사용자는 L1~L5 중 어느 등급 근거가 있는지 명시한 뒤 게이트 결정함 ([[AGENTS#2.4 사용자 승인 게이트]] 참조)
- "합리적으로 보임"은 사유 아님: 외부 근거가 있어야 통과 ([[codex 검수 포커스]] 기조 유지)
- L1 변경은 사용자 명시 승인 필수: `_locked/` read-only 정책([[#6. 수정 금지 영역]])과 직결

## 3.8 구조 우선 작성 (정보 설계)

단순 문장 나열 금지. 바로 본문을 쓰지 말고, 내용을 분류하고 구조를 설계한 뒤 작성함. 작성자가 반복적으로 평문 나열, 라벨에 서너 줄을 욱여넣은 1단계 불릿, 비병렬 구조를 낸 데 대한 보강 (사용자 제공 「한국어 문서 작성 지침」 통합, 2026-06-09). 본 절은 [[#3.3 본문 구조]], [[#3.4 문체]]와 함께 적용함. 문서 유형별 기본 구조, 구조화 규칙, 금지, 목록 형식 선택(라벨형과 중첩 불릿)의 정본은 [[문서 작성 세부 규칙]]임 (2026-08-21 이관, 상시 로드 감량).

### 3.8.1 작성 전 절차

본문을 쓰기 전에 수행함.

1. 독자와 목적 파악: 누가 읽고, 읽은 뒤 무엇을 이해하거나 실행해야 하는가
2. 의미 단위 분류: 내용을 정의, 배경, 목적, 핵심 개념, 절차, 기준, 예외, 주의, 예시, 결론, 다음 행동 등으로 나눔
3. 목차 먼저 설계: 분류에 맞는 헤딩과 항목 구조를 잡고 나서 본문을 채움
4. 작성 후 재구조화: 문장 나열처럼 보이는 곳을 헤더, 목록, 표, sub-bullet로 다시 나눔

## 3.9 모순(contradiction) 처리

기존 지식과 충돌하는 새 정보는 삭제하지 않고 블록으로 보존함. 어느 쪽이 맞는지 정해지기 전까지 양쪽을 함께 남겨 판단 근거를 잃지 않게 함.

```markdown
> [모순] A는 X라 함 ([[출처1]]). B는 X가 아니라 함 ([[출처2]]). 미해결.
```

- **정본 충돌은 L1 우선**: 본문이 `_locked/`(L1) 정본과 어긋나면 모순이 아니라 본문 오류임. L1에 맞춰 정정함 ([[CONVENTIONS#3.7 외부 근거의 권위 계층]])
- **정량 수치 충돌**: 원문 간 수치가 충돌하면 양쪽을 표기하고 사용자에게 확인을 요청함 ([[#3.6 수치/목표 인용 원칙 (엄격)]] 4)
- **해소 시**: 모순이 풀리면 블록을 정리하고 log에 경위를 남김

# 4. 헤딩 / 넘버링

## 4.1 헤딩 레벨 사용 규칙

- **H1(`#`) = 본문 최상위 절**. 문서 제목용으로 쓰지 않음: frontmatter `title`이 제목원
- 본문은 TL;DR 인용블록 뒤 H1(`#`)부터 시작
- 깊이 한계: H5(`#####`)까지 사용. 위키층 문서는 원자 단위로 쪼개 쓰므로 보통 H3 안에서 끝남. H5에 닿으면 문서를 나누라는 신호임. 더 깊은 구조가 필요하면 문서를 분리하거나 표/불릿으로 평탄화
- **제목은 가리키기만 하고 주장하지 않음**: 절 제목에 승패나 결말 같은 사실을 담지 않음. 제목이 사실을 주장하면 제목부터 틀릴 수 있음. 전사본을 다루는 문서에서는 `scripts/quote_check.mjs`가 주장 어휘(`scripts/heading_claim_terms.txt` 등록분)가 제목에 있으면 경고함 (2026-08-21 신설, 어휘 매칭이라 오탐이 있어 막지는 않음)

## 4.2 헤딩 넘버링

헤딩 prefix로만 사용함. 본문 일반 단락에는 적용 안 함(마크다운 ordered list 자동 넘버링 충돌 회피). **번호는 손으로 적지 않고 스크립트가 매김** ([[#4.3 번호 자동 부여]]).

| 레벨 | 표기 | 예시 |
|---|---|---|
| H1 | `1.` `2.` `3.` | `# 1. 개요` |
| H2 | `1.1` `1.2` `1.3` | `## 1.1 핵심 내용` |
| H3 | `1.1.1` `1.1.2` `1.1.3` | `### 1.1.1 세부 항목` |
| H4 | `1)` `2)` `3)` | `#### 1) 보충 설명` |
| H5 | `(1)` `(2)` `(3)` | `##### (1) 역할과 목적` |

H1부터 H3까지는 상위 번호를 품어, 헤딩만 보고 문서 안에서 어디쯤인지 알 수 있음. anchor에도 경로가 그대로 드러남(`[[CONVENTIONS#4.2 헤딩 넘버링]]`). H4 이하는 상위와 이어지는 경로가 아니라 그 자리의 열거라 번호를 짧게 끊음.

옛 `한국 공문서 넘버링`(`가.` `1)` `가)`)은 2026-07-14에 폐기함. H2에 쓰던 한글 기호가 `가~하` 14개로 상한이 있었고(실제로 12개까지 찬 문서가 있었음), 레벨마다 형식이 달라 사람이 순서를 세다 틀리는 결함이 반복됐음.

**예외**

- `# 0. 문서 정보` (버전 관리표 절): **산출물층(`05_산출물/`) 규칙임.** 위키층 문서는 두지 않음. `wiki_number`가 첫 H1 제목이 "문서 정보"면 `0.`을 주고 다음 H1부터 1로 세는 지원만 함
- `99_Logs/log.md`: 두 가지 예외 동시 적용:
	- 헤딩은 `## [YYYY-MM-DD] {유형} | {제목}` 양식 유지([[#7. 로그 항목 양식]]). Unix 도구 파싱 호환 우선, 넘버링 적용 안 함
	- H1 폐지 미적용: 본문 상단 `# 작업 로그` H1 유지. 로그 항목들이 H2로 진행되는 구조의 컨테이너 헤딩 역할
- `01_References/_locked/`, `01_References/converted/`, `01_References/_reviews/`: 변환 산출물. 외부 원본 양식 보존, 별도 변환 파이프라인이 생성/관리. **영구 자산은 `_reviews/`에 두지 않음**: 일시 자산 폴더 정의([[#6. 수정 금지 영역]]) 위배. 영구 운영 노트는 `01_References/` 루트 등 영구 위치에 둠

## 4.3 번호 자동 부여

번호는 `scripts/wiki_number.mjs`가 매김. 절을 새로 넣거나 빼도 사람이 뒷번호를 다시 세지 않음.

- **부여**: `node scripts/wiki_number.mjs --write`. 헤딩을 번호 없이 써 두면 규칙대로 붙고, 이미 붙은 번호는 떼고 다시 매김(같은 결과가 나오므로 여러 번 돌려도 안전함)
- **점검**: `node scripts/wiki_number.mjs --check`. 어긋난 헤딩이 있으면 exit 1
- **anchor 연쇄 갱신**: 번호가 바뀐 헤딩을 가리키는 wikilink를 저장소 전체에서 함께 고침. 자기 문서 `[[#...]]`, 임베드 `![[...]]`, 별칭형을 모두 포함하고, `04_Projects/_archive/`의 링크도 대상임(본문은 안 건드리고 링크만)
- **손이 필요한 경우**: 한 문서에 같은 헤딩 텍스트가 둘 이상이면 anchor를 어느 쪽으로 고칠지 정할 수 없어 스크립트가 경고만 하고 넘어감. 그때는 사람이 확정함
- **깊은 중첩 회피**: H5에 닿으면 문서 분리나 평탄화를 검토함

# 5. 문서 작성/갱신 절차

## 5.1 문서 생성 시

1. 적절한 디렉토리에 파일 생성
2. 프론트매터 포함 (`title` 단일 제목원, `type`과 `status` 지정, [[#3.2 프론트매터 (YAML)]])
3. 본문은 TL;DR 뒤 H1(`#`)부터 시작: H1 제목 라인 금지
4. 헤딩 번호는 손으로 붙이지 않고 `node scripts/wiki_number.mjs --write`가 매김 ([[#4.3 번호 자동 부여]])
5. 기존 관련 문서에 wikilink 추가 (양방향 연결)
6. MOC(00_Index) 문서에 새 항목 등록

## 5.2 문서 갱신 시

1. 기존 내용을 먼저 읽고 파악
2. `updated` 날짜 갱신
3. 기존 구조를 존중하며 내용 추가/수정
4. 본문이 바뀐 절의 파생 텍스트(절 제목, 라벨, 표 셀, TL;DR, MOC 요약)가 여전히 맞는지 함께 봄 (2026-08-21 추가: 본문만 고치고 이름표를 안 고쳐 생긴 stale이 반복됨)
5. 헤딩 추가/삭제 시 후속 번호 재조정 + 다른 문서 anchor 갱신
6. 새로운 연결이 생기면 wikilink 추가

# 6. 수정 금지 영역

**`_xxx/` (언더스코어 접두) 패턴 폴더는 read-only로 취급함.** 정본/원본/아카이브 등 사람 외부 권위 또는 사용자가 봉인한 자료가 담기며, 자율 편집 대상이 아님.

현재 적용 대상:

- `01_References/_locked/`: 정본 (RFP 등 외부 권위 자료 또는 사용자 봉인 확정본)
- `01_References/_sources/`: 변환 원본 (DOCX, HWPX 등 변환 파이프라인의 입력)
- `01_References/_figures/`: 변환 추출 이미지 (변환 파이프라인 외 수정 금지)
- `01_References/_reviews/`: 변환 검수 큐 사이드카 (변환 파이프라인이 생성, 검수 완료 후 사용자가 삭제)
- `04_Projects/_archive/`: 폐기 문서 (`status: deprecated`, 미승인이거나 효력 상실). 이력 보존용. 폐기 사유와 정본 안내는 본문 상단 `> [폐기]` 블록에 적음

규칙:

- 내용 변경, 재작성, 파일 이동/삭제 금지
- 읽기(Read)와 인용은 허용
- 변환 파이프라인이 `_sources/`에 파일을 **추가**하는 것은 허용 (수정/삭제는 금지)
- 수정이 필요하다고 판단되면 사용자에게 먼저 확인하고 승인받은 후에만 작업
- 향후 새 `_xxx/` 폴더가 추가되면 동일 규칙 자동 적용
- `_xxx/` 폴더 내부 파일/구조는 폴더마다 다르므로 작업 시 디렉토리 목록을 그때그때 확인

# 7. 로그 항목 양식

`99_Logs/log.md`에 추가되는 항목은 다음 양식을 따름:

```markdown
## [YYYY-MM-DD] {작업유형} | {제목 또는 주제}

- 작업 내용 요약
- 생성/수정된 파일 목록
- 새로 생성된 wikilink
```

**작업유형 접두사**

- `ingest`: 새 소스(기사, 논문, URL 등)를 읽고 위키에 통합
- `query`: 위키를 대상으로 질문하고 답변을 생성 (위키로 환원 시 명시)
- `create`: 새 문서 생성
- `update`: 기존 문서 수정
- `lint`: 위키 상태 점검 (모순, 고아 페이지, 누락 링크 등)
- `maint`: 구조나 스키마 등 기타 정비
- `review`: Codex 검수 결과 반영

**양식 규칙**

- 로그는 최신 항목이 **위**에 오도록 역순으로 추가함
- Unix 도구로 파싱 가능하도록 `## [날짜]` 형식을 정확히 지킴
- [[#4.2 헤딩 넘버링]]의 헤딩 넘버링을 **적용 안 함** (예외 명시)

# 8. 관련 문서

- [[AGENTS|에이전트 작업 지침]]: AI 운영 규칙 (역할, 사이클, 게이트, 검수)
- [[문서 작성 세부 규칙]]: 문서 구조화 상세 (유형별 기본 구조, 구조화 규칙, 금지, 목록 형식 선택) (2026-08-21 이관)
- [[위키 운영 워크플로]]: 수집/질의/유지 절차와 Overview 페이지 (2026-08-21 이관)
