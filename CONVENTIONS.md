---
title: LLM Wiki 작성 규칙
tags: [convention, wiki]
created: 2026-04-27
updated: 2026-06-05
---

이 위키의 디렉토리 구조, 문서 작성 규칙, 메타데이터, 인용 원칙, 수정 금지 영역, 로그 양식을 정의함. AI 운영 규칙(역할, 사이클, 게이트, 검수)은 별도 [[AGENTS|에이전트 작업 지침]]에서 다룸. 본 문서는 AI/사람 누구든 위키에 글을 쓸 때 따르는 표준임. AGENTS.md가 본 문서를 `@CONVENTIONS.md`로 import 함.

# 1. 핵심 원칙

이 위키는 Andrej Karpathy의 **LLM Wiki** 패턴을 따르는 **프로젝트 지식 베이스**임. 원본을 질문마다 다시 읽는 RAG 대신, **수집(ingest) 시점에 지식을 위키로 컴파일**하고 시간이 지날수록 **복리(compounding)로 축적**함. LLM(Claude)이 마크다운 문서를 직접 작성/편집/연결하는 **위키 에디터** 역할을 함 ([[AGENTS]] §1). 다만 외부 산출물(기획서/제안서 등)을 내는 과제 저장소라, 기본 패턴에 정본(`_locked`) 재대조와 엄격 인용(§3.바, §3.사), 다각도 검증([[AGENTS]] §2)을 더한 변형임.

1. **Atomic Notes**: 하나의 파일에 하나의 주제. 파일명이 곧 제목.
2. **Wikilinks**: 문서 간 연결은 `[[문서명]]` 형식 (Obsidian 호환).
3. **점진적 축적**: 한 번에 완벽한 문서를 만들지 않고, 대화를 거듭하며 보강함.
4. **한국어 우선**: 문서는 한국어로 작성. 기술 용어는 영문 병기 가능. 영어 단독 작성 금지.

# 2. 디렉토리 구조

```
/                     # vault root
├── AGENTS.md         # 에이전트 작업 지침 (AI 운영 규칙)
├── CONVENTIONS.md    # 이 문서 — 위키 작성 규칙
├── CLAUDE.md         # Claude Code 자동 로드 진입점 (AGENTS.md import 스텁)
├── 00_Index/         # 목차, 지도 문서 (MOC: Map of Content)
├── 01_Concepts/      # 개념 정리 (정의, 원리, 이론)
├── 02_HowTo/         # 실용 가이드, 레시피, 튜토리얼
├── 03_References/    # 논문 요약, 링크, 인용
│   ├── _locked/      # 정본 (RFP, 확정본). 에이전트 read-only
│   ├── _sources/     # 변환 원본 (DOCX, HWPX 등). 에이전트 read-only
│   ├── _figures/     # 변환 추출 이미지 (영구 자산). 변환 파이프라인만 생성/덮어쓰기
│   ├── _reviews/     # 변환 검수 큐 사이드카 (일시 자산, 검수 후 삭제). 변환 파이프라인만 생성
│   └── converted/    # 변환 본문 .md (1차 탐색 대상). 에이전트 수정 가능
├── 04_Projects/      # 프로젝트별 노트
├── 05_Logs/          # 학습 일지, 실험 기록
├── scripts/          # 자동화 스크립트 (변환 파이프라인 등)
└── assets/           # 이미지, 첨부파일
```

변환 산출 바이너리 파일은 `assets/` 또는 `03_References/_figures/` 외 위치에 두지 않음.

# 3. 문서 작성 규칙

## 가. 파일명

- 공백 대신 언더스코어(`_`) 사용 금지 — Obsidian은 공백 허용
- 한국어 파일명 사용 가능 (예: `트랜스포머.md`)
- 영문 약어가 보편적인 경우 영문 사용 (예: `LLM.md`, `RAG.md`)

## 나. 프론트매터 (YAML)

모든 문서 상단에 다음 메타데이터를 포함함. **`title` 필드가 문서의 유일한 제목원** — 본문에 H1 제목 재기재 금지(§4 헤딩 / 넘버링 참조).

```yaml
---
title: 문서 제목
tags: [태그1, 태그2]
created: YYYY-MM-DD
updated: YYYY-MM-DD
---
```

## 다. 본문 구조

H1 제목 라인은 두지 않음 — frontmatter `title`이 단일 제목원. 본문은 한 줄 요약(TL;DR) 인용블록부터 시작하고, 섹션은 H1(`#`)부터 사용함.

```markdown
> 한 줄 요약 (TL;DR)

# 1. 핵심 내용

- ...

# 2. 세부 설명

- ...

# 3. 관련 문서

- [[관련문서1]]
- [[관련문서2]]

# 4. 참고

- 출처, URL 등
```

## 라. 문체

### 1) 기본 문체

- **두괄식**: 결론, 핵심을 먼저 쓰고, 부연은 뒤에 배치
- **개조식**: 불릿 포인트 중심으로 간결하게 서술
- **음슴체**: "~이다", "~한다" 대신 "~임", "~함" 체를 사용
- 한 문장은 짧게, 불필요한 수식어 제거
- 표, 리스트를 적극 활용하여 가독성 확보
- **가운뎃점(`·`) 사용 금지**: 항목 나열은 쉼표(`,`) 또는 슬래시(`/`)로 대체
- **em dash(`—`, U+2014) 사용 금지**: 부연/삽입은 쉼표(`,`), 콜론(`:`), 괄호로 대체하거나 문장을 나눔

### 2) 쉽게 쓰기

어려운 말보다 쉬운 말. 읽는 이(미래의 나, 위키를 다시 읽을 사용자)가 한 번에 이해하게 씀.

- **쉬운 말 우선**: 어려운 한자어/외래어 대신 친숙한 우리말. 예: 제고함 → 높임, 소정의 → 정해진, 동 사업 → 이 사업, 타 기관 → 다른 기관. 단 기술 표준 용어(transformer, embedding 등)는 영문 병기 허용(§1 핵심 원칙 4)
- **약어 첫 등장 시 본디 용어 병기**: 줄임말 남용 금지. 예: 검색 증강 생성(RAG), 사업 모델(BM)
- **번역 투, 과도한 피동/사동 절제**: 예: 변경됨에 의해 → 변경됨, 환기를 시켜야 → 환기를 해야
- **중복/중의/군더더기 제거**: 같은 뜻 반복 금지(사전 동의가 반드시 필요 → 사전 동의가 필요), 한 가지 뜻으로만 읽히게 씀

### 3) 표기 정확성과 어문 규범

- **맞춤법/표준어 준수**: 예: 년도 → 연도, 아니오 → 아니요(가부 응답)
- **띄어쓰기 준수**: 예: 1천만원 → 1천만 원, 기재여부 → 기재 여부
- **외래어 표기법 준수**: 예: 마켓팅 → 마케팅
- **어문 규범 참고처**: 표준국어대사전/우리말샘(표준어/표기 확인), 국립국어원 한국어 어문 규범(맞춤법/띄어쓰기/외래어 표기법 원문)
- 본 2)~3) 규칙은 국립국어원 「쉬운 공문서 쓰기 길잡이」(2022) 공공언어 작성 원칙을 위키용으로 재구성 (L4 외부 근거, [[CONVENTIONS#사. 외부 근거의 권위 계층]])

## 마. Wikilink 규칙

- 새 개념이 등장하면 `[[개념명]]`으로 링크를 검
- 링크 대상 문서가 아직 없어도 괜찮음 (나중에 생성)
- 별칭이 필요하면 `[[파일명|표시텍스트]]` 형식 사용
- 섹션 anchor는 `[[파일명#H1번호. 섹션텍스트]]` 형식 (예: `[[CONVENTIONS#1. 핵심 원칙]]`). H2 이하 anchor는 한국 공문서 넘버링(가./1)/가))을 포함한 헤딩 텍스트 전체를 그대로 씀

**Wikilink vs § 표시 — 참조 표기 위계**

| 참조 종류 | 권장 표기 | 이유 |
|---|---|---|
| 외부 문서 + 헤딩 | `[[파일명#섹션]]` wikilink anchor (필수) | 클릭 가능, 자동 stale 검출(wiki_lint), 양방향 backlink |
| 외부 문서 + 절번호 | `[[파일명]] §X` (wikilink + § 조합) | 외부 문서 명시 + § 절번호 가독성 |
| 자기 문서 내 절 참조 | `§X` 또는 `§X.Y` (§ 표시 허용) | 짧고 가독성 ↑. 다만 자동 stale 검출 어려움 |

**§ 표시 한계 + 의무**

- 자기 문서 § 표시는 본문 자연어에 녹아 있어 정확한 라벨 경계 자동 검출이 NLP 수준 필요. **wiki_lint는 § stale을 자동 검출하지 않음** (false positive 과다로 v2 § 검증 폐기됨)
- 절 라벨 변경 시 작성자는 **본문 § 참조를 grep으로 수동 점검** 의무 (예: `Grep "§4 시스템 인벤토리"` 후 잔류 정정)
- 더 안전한 정합을 원하면 자기 문서 내 참조도 wikilink anchor 사용 가능 (`[[#1. 핵심 원칙]]` Obsidian 형식)
- **외부 문서 참조에 § 단독 사용 금지** — `§4`만 적으면 어느 문서인지 모호. 무조건 wikilink + § 조합

## 바. 수치/목표 인용 원칙 (엄격)

문서에 등장하는 모든 **구체적 수치**와 **목표**는 반드시 `03_References/` 하위 원문을 직접 참조하여 작성함. 임의 추정/창작 금지.

**대상 항목**

- 정량 지표: 예산, 인원, 기간, 페이지 수, 비율(%), 점수, KPI, TRL 등
- 목표 진술: 사업 목표, 성과 목표, 정량/정성 목표, 핵심 성과지표
- 일정/기한: 착수일, 종료일, 단계별 마일스톤, 마감 일자
- 조직 정보: 컨소시엄 구성, 역할 분담, 기관별 담당 비율
- 산출물 명세: 결과물 수량, 규격, 인도 조건

**작성 절차**

1. 수치/목표를 적기 전에 `03_References/` 하위 원문(특히 `_locked/RFP.md`, `_locked/컨소시엄 별 역할.md`)을 먼저 확인
2. 문서 내 인용 시 출처를 wikilink로 명시 — 예: `[[RFP]] §3.2`, `[[컨소시엄 별 역할#수행기관]]`
3. 원문에 해당 정보가 없으면 **추정 금지** — `원문 미명시` 또는 `확인 필요`로 표기
4. 원문 간 수치가 충돌하면 양쪽을 모두 표기하고 사용자에게 확인 요청
5. 외부 자료(웹, 일반론)에서 가져온 수치는 과제 고유 수치와 명확히 구분 표기 (출처 URL 포함)

**금지 사항**

- 그럴듯해 보인다는 이유로 임의 수치 제시 금지
- 시장 평균치/업계 통상치를 과제 고유 수치인 양 기재 금지
- 출처 없이 "약 N억 원", "N개월", "N명 규모" 같은 어림값 작성 금지
- 원문 수치를 단위 변환/반올림하여 적을 때도 원문 표기를 함께 병기

## 사. 외부 근거의 권위 계층

문서의 주장과 검수 토론에서 "외부 근거"의 권위를 5단계로 구분함. L1이 가장 강력, L5가 가장 약함. 충돌 시 상위 등급이 우선.

| 등급 | 출처 | 인정 범위 |
|---|---|---|
| L1 정본 | `03_References/_locked/`, `03_References/_sources/` 원본 | 수치/일정/정의의 최종 근거. L1과 충돌 시 본문이 틀린 것 |
| L2 변환본 | `03_References/converted/` | L1에서 파생. L1 재대조 가능할 때만 인용. L1과 어긋나면 L1 우선 |
| L3 내부 결정 | `04_Projects/` 내 사용자 확정 결정사항 | 본 저장소 내부에서만 유효. 외부 노출 시 정본 정합성 재확인 |
| L4 외부 자료 | 웹, 일반 문헌, 통상치 | 인용 시 URL 명시 + 과제 고유 수치와 명확히 구분. 정본 대체 불가 |
| L5 추론 | 에이전트 추론 | "추정", "확인 필요" 표기 의무. L1~L4 근거 없이 본문 단정 금지 |

**적용 규칙**

- 검수 발견에서 "근거 약함" 플래그가 나오면 사용자는 L1~L5 중 어느 등급 근거가 있는지 명시한 뒤 게이트 결정함 ([[AGENTS#라. 사용자 승인 게이트|AGENTS §2.라 사용자 승인 게이트]] 참조)
- "합리적으로 보임"은 사유 아님 — 외부 근거가 있어야 통과 ([[codex 검수 포커스]] 기조 유지)
- L1 변경은 사용자 명시 승인 필수 — `_locked/` read-only 정책(§수정 금지 영역)과 직결

# 4. 헤딩 / 넘버링

## 가. 헤딩 레벨 사용 규칙

- **H1(`#`) = 본문 최상위 절**. 문서 제목용으로 쓰지 않음 — frontmatter `title`이 제목원
- 본문은 TL;DR 인용블록 뒤 H1(`#`)부터 시작
- 깊이 한계: H4(`####`)까지 사용. 더 깊은 구조가 필요하면 문서를 분리하거나 표/불릿으로 평탄화

## 나. 한국 공문서 넘버링

헤딩 prefix로만 사용함. 본문 일반 단락에는 적용 안 함(마크다운 ordered list 자동 넘버링 충돌 회피).

| 레벨 | 표기 | 예시 |
|---|---|---|
| H1 | `1. 2. 3. …` (마침표) | `# 1. 개요` |
| H2 | `가. 나. 다. …` (마침표) | `## 가. 핵심 내용` |
| H3 | `1) 2) 3) …` (괄호) | `### 1) 세부 항목` |
| H4 | `가) 나) 다) …` (괄호) | `#### 가) 보충 설명` |

**예외**

- `05_Logs/log.md` — 두 가지 예외 동시 적용:
	- 헤딩은 `## [YYYY-MM-DD] {유형} | {제목}` 양식 유지(§7 로그 항목 양식). Unix 도구 파싱 호환 우선, 넘버링 적용 안 함
	- H1 폐지 미적용 — 본문 상단 `# 작업 로그` H1 유지. 로그 항목들이 H2로 진행되는 구조의 컨테이너 헤딩 역할
- `03_References/_locked/`, `03_References/converted/`, `03_References/_reviews/` — 변환 산출물. 외부 원본 양식 보존, 별도 변환 파이프라인이 생성/관리. **영구 자산은 `_reviews/`에 두지 않음** — 일시 자산 폴더 정의(§6) 위배. 영구 운영 노트(예: 변환본 모순점 정리)는 `03_References/` 루트 등 영구 위치에 둠

## 다. 운영상 유의

- 신규 섹션 추가/삭제 시 같은 레벨의 후속 번호 모두 재조정 필요
- 헤딩 텍스트 변경 시 다른 문서의 wikilink anchor도 함께 갱신 필요 (전체 grep으로 일괄 처리)
- 깊은 중첩 회피 — H4 도달 시 분리/평탄화 검토

# 5. 문서 작성/갱신 절차

## 가. 문서 생성 시

1. 적절한 디렉토리에 파일 생성
2. 프론트매터 포함 (`title`을 단일 제목원으로)
3. 본문은 TL;DR 뒤 H1(`#`)부터 시작 — H1 제목 라인 금지
4. 한국 공문서 넘버링 적용 (§4 나)
5. 기존 관련 문서에 wikilink 추가 (양방향 연결)
6. MOC(00_Index) 문서에 새 항목 등록

## 나. 문서 갱신 시

1. 기존 내용을 먼저 읽고 파악
2. `updated` 날짜 갱신
3. 기존 구조를 존중하며 내용 추가/수정
4. 헤딩 추가/삭제 시 후속 번호 재조정 + 다른 문서 anchor 갱신
5. 새로운 연결이 생기면 wikilink 추가

# 6. 수정 금지 영역

**`_xxx/` (언더스코어 접두) 패턴 폴더는 read-only로 취급함.** 정본/원본/아카이브 등 사람 외부 권위 또는 사용자가 봉인한 자료가 담기며, 자율 편집 대상이 아님.

현재 적용 대상:

- `03_References/_locked/` — 정본 (RFP 등 외부 권위 자료 또는 사용자 봉인 확정본)
- `03_References/_sources/` — 변환 원본 (DOCX, HWPX 등 변환 파이프라인의 입력)
- `03_References/_figures/` — 변환 추출 이미지 (변환 파이프라인 외 수정 금지)
- `03_References/_reviews/` — 변환 검수 큐 사이드카 (변환 파이프라인이 생성, 검수 완료 후 사용자가 삭제)

규칙:

- 내용 변경, 재작성, 파일 이동/삭제 금지
- 읽기(Read)와 인용은 허용
- 변환 파이프라인이 `_sources/`에 파일을 **추가**하는 것은 허용 (수정/삭제는 금지)
- 수정이 필요하다고 판단되면 사용자에게 먼저 확인하고 승인받은 후에만 작업
- 향후 새 `_xxx/` 폴더(`_archive/` 등)가 추가되면 동일 규칙 자동 적용
- `_xxx/` 폴더 내부 파일/구조는 폴더마다 다르므로 작업 시 디렉토리 목록을 그때그때 확인

# 7. 로그 항목 양식

`05_Logs/log.md`에 추가되는 항목은 다음 양식을 따름:

```markdown
## [YYYY-MM-DD] {작업유형} | {제목 또는 주제}

- 작업 내용 요약
- 생성/수정된 파일 목록
- 새로 생성된 wikilink
```

**작업유형 접두사**

- `ingest` — 새 소스(기사, 논문, URL 등)를 읽고 위키에 통합
- `query` — 위키를 대상으로 질문하고 답변을 생성
- `create` — 새 문서 생성
- `update` — 기존 문서 수정
- `lint` — 위키 상태 점검 (모순, 고아 페이지, 누락 링크 등)
- `review` — Codex 검수 결과 반영

**양식 규칙**

- 로그는 최신 항목이 **위**에 오도록 역순으로 추가함
- Unix 도구로 파싱 가능하도록 `## [날짜]` 형식을 정확히 지킴
- §4 헤딩/넘버링 규칙의 한국 공문서 넘버링 **적용 안 함** (예외 명시)

# 8. 관련 문서

- [[AGENTS|에이전트 작업 지침]] — AI 운영 규칙 (역할, 사이클, 게이트, 검수)
