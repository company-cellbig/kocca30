---
title: 외부 검수 포커스 텍스트
type: howto
status: stable
tags: [howto, review, claude, workflow]
created: 2026-05-11
updated: 2026-08-23
---

> Codex 작성자가 검수 패킷에 첨부하고 Claude Code 외부 검토자가 fresh context에서 사용하는 표준 비판적 검수 프롬프트임.

# 1. 배경

이 저장소는 마크다운 문서가 주요 산출물이므로 일반 코드 검토 대신 문서의 사실성, 논리성, 가독성, 위키 정합성을 우선하는 포커스 텍스트가 필요함. Codex가 작성하고 Claude Code가 외부 검토하므로 도구 고유 명령이 아닌 검수 패킷으로 전달함.

추가 근거: LLM-as-judge는 명시적 비판 프롬프트 없이는 관대해지는 경향이 있음. 회의 기조와 우선순위/평가 기준을 명시할수록 catch율이 올라감.

# 2. 표준 포커스 텍스트

검수 패킷 예시 ([[AGENTS#3. 검수 기준과 호출 방식]] 참조):

```
검토 종류: 사실/정합
검토 지침: <아래 머리말 + P1 + P5 + 끝맺음>
대상: <커밋 해시, 변경 파일, 범위>
근거: <원자료 경로 또는 해당 없음>
```

```
이 저장소는 LLM Wiki(마크다운 문서 저장소). 다음 우선순위로 비판적 검수
수행. "괜찮아 보임"은 사유가 아님: 결함을 적극적으로 찾을 것.
대조가 필요한 항목은 첨부된 비교 원문의 범위에서만 수행하고, 첨부가
없으면 그 항목을 "검증 불가"로 보고할 것.

[P1 사실 오류/출처 미스매치]
- 수치/일정/인용이 출처(_locked 또는 wikilink 대상) 원문과 일치하는가
- 출처 없이 들어간 정량 주장이 있는가

[P2 논리적 비약/추론 정정]
- 합계/누적/인과 관계에 비약 (예: 연차별 합 ≠ 총량)
- 한 문서 단정 사실이 다른 문서/출처와 모순
- A→B, B→A 상호 참조에서 양쪽 다 "상세는 저쪽"으로 미뤄 실체(정의/값/명세)가 부재한 순환 참조인가 (각자 실체 보유한 정상 정본 분담과 구별)

[P3 누락된 반론/견고성]
- 외부 검증 없이 "확정" 표기된 추정값
- 원문 모순이 정상화되어 감사 추적이 사라졌는가
- 주장에 한계 조건이 빠져 있는가

[P4 톤/구조 일관성 (CONVENTIONS.md 3.4 문체)]
- 음슴체(~임/~함) 준수, 가운뎃점(`·`) 금지, em dash 금지, 두괄식/개조식

[P5 위키 정합성]
- 참조가 실제로 그 내용을 담은 곳을 가리키는가 (제목만 맞고 내용이 없는 참조)
- 상호 참조가 서로 미루기만 해 실체가 어느 쪽에도 없지 않은가
- 깨진 링크, anchor 정합, MOC 등록은 scripts/wiki_lint.mjs가
  기계로 잡으므로 여기서 보지 않음

기조: 회의가 기본. 단순 "합리적으로 보임"은 approve 사유가 아님:
외부 근거가 있어야 통과. CONVENTIONS.md 3.6 수치/목표 인용 원칙, 3.7
외부 근거의 권위 계층 및 AGENTS.md 3. 검수 기준과 호출 방식 우선 가이드.
```

# 3. 발췌 종류별 가이드 (사실/정합, 적대, 가독성)

[[AGENTS#2.1 역할 분담]]의 외부 검토자(Claude Code)는 발췌 종류로 시선을 분리함. 단일 검토에 P1~P5 전체를 넣으면 P1에 쏠리고 P2/P3가 묻힐 수 있음. 필요한 발췌를 같은 검수 패킷에 넣되 발견은 종류별로 구분함.

## 3.1 사실/정합 발췌 (P1 + P5)

- 머리말 (회의 기조 + "괜찮아 보임" approve 사유 아님 문장)
- [P1 사실 오류/출처 미스매치] 블록 전문
- [P5 위키 정합성] 블록 전문
- 끝맺음 ("[[CONVENTIONS#3.6 수치/목표 인용 원칙 (엄격)]], [[CONVENTIONS#3.7 외부 근거의 권위 계층]] 및 [[AGENTS#3. 검수 기준과 호출 방식]] 우선 가이드" 문장)

패킷 예시:

```
검토 종류: 사실/정합
검토 지침: <머리말 + P1 + P5 + 끝맺음>
```

**시선**: 출처가 원문과 일치하고 참조가 실체에 닿는가. 깨진 링크, anchor 정합, MOC 등록은 `scripts/wiki_lint.mjs`가 기계로 잡으므로 여기서 시키지 않음. Claude Code는 검수 패킷에 적힌 저장소 파일과 원자료를 직접 열어 대조함.

## 3.2 적대 발췌 (P2 + P3 + 반복 결함 카탈로그)

- 머리말 (회의 기조)
- [P2 논리적 비약/추론 정정] 블록 전문
- [P3 누락된 반론/견고성] 블록 전문
- 끝맺음
- **추가 첨부**: [[반복 결함 카탈로그]] 본문: 과거 반복 결함의 재발 우선 점검 지시

패킷 예시:

```
검토 종류: 적대
검토 지침: <머리말 + P2 + P3 + 끝맺음 + 반복 결함 카탈로그 본문>
```

**시선**: 논리 비약, 누락 반론, 한계 조건. 회의 기조 강화. 작성자가 자기 작업에 관대해지는 사각지대 보완.

## 3.3 가독성 발췌 (P4 + 명료성, 낯선 독자 시선)

작성자가 자기 글에서 발견하기 어려운 모호한 용어, 구어체와 반복을 새 Claude Code 세션에서 외부 시선으로 찾음. 작은 변경은 Codex 서브에이전트의 독립 맥락 검토로 대체할 수 있음.

**첨부 블록**: 머리말(회의 기조) + 아래 [가독성/명료성] 블록 + 끝맺음

**전달 방법**: Codex가 검수 패킷에 첨부 블록, 대상 커밋과 파일을 적고 사용자가 새 Claude Code 세션에 전달함.

```
[가독성/명료성 (낯선 독자 시선)]
맥락 없는 독자로 각 문장을 읽고 다음을 적극 지적:
- 정의 안 된 용어나 지시어 (예: "분석"이 무슨 분석인지 안 적음, 막연한 "이 기능", "해당 값")
- 구어체, 비표준어, 임의 조어 (예: "이름 없는 토큰" 같은 비표준 표현)
- 같은 뜻을 다른 문장으로 반복하는 군더더기
- 단순 문장 나열 (의미 분류와 항목화 안 됨, 라벨에 서너 줄 욱여넣음)
- 1단계 불릿에 복수 항목(역할/단계/종류별) 욱여넣기, 제목이 복수 주제('A와 B')인데 본문 평면 나열
- 목록 형식 오선택 (조건-결과인데 평문, 담당자별로 갈리는데 한 불릿, 길고 이질적 값인데 표로 셀 과적)
- 같은 수준 항목의 비병렬 구조 (문법 형태 제각각)
- 모호한 항목명("내용", "기타", "참고")만 반복
- 음슴체, 가운뎃점, em-dash, 슬래시 절제 위반 (CONVENTIONS 문체와 구조 우선 작성)
기준: 처음 보는 개발자나 심사자가 한 번에 이해하는가.
```

**분담**: 작은 변경은 Codex 서브에이전트 독립 검토가 1차 방어이고, 큰 변경과 외부 노출 정본은 Claude Code에 본 발췌를 보냄. 작성자가 자기 글을 직접 낯선 독자로 읽는 것은 독립 검토로 세지 않음.

# 4. 우선순위 설계 근거

| 층위 | 의미 | 검출 사례 (가설) |
|---|---|---|
| P1 사실 오류 | 가장 비싼 결함: 잘못된 수치가 산출물로 나가는 위험 | RFP 인용 수치 변조 |
| P2 논리적 비약 | 그럴듯한 추론이 검증된 사실로 둔갑하는 위험 | _locked 추론 정정, 합산 모순 |
| P3 누락 반론 | 작성자가 자기 작업에 관대해지는 사각지대 | "확정" 표기된 추정, 한계 조건 누락 |
| P4 톤/구조 | 점진 부식: 개별 위반은 사소하나 누적되면 위키 가치 훼손 | 음슴체 위반, 라벨 과적 |
| P5 위키 정합성 | 참조가 실체에 닿는가 | 제목만 맞는 참조, 상호 미루기로 빈 참조 |

# 5. 검수 패킷 운용 규칙

- Codex가 커밋 해시, 변경 파일과 범위, 검토 종류, 발췌, 원자료 경로, 금지 사항과 출력 형식을 채팅에 적음
- 사용자가 패킷을 새 Claude Code 세션에 전달함. 작성 대화, 작성 의도와 중간 추론은 전달하지 않음
- Claude Code는 파일을 수정하지 않고 위치, 문제, 근거, 영향, 제안으로 발견을 보고함
- 사실/정합, 적대, 가독성 발췌를 한 패킷에 함께 넣을 수 있으나 발견은 종류별로 구분함
- 단일 검수가 필요한 소규모 변경이나 임시 점검에는 표준 포커스 텍스트 전문을 첨부할 수 있음

# 6. 관련 문서

- [[AGENTS#2.1 역할 분담]], [[AGENTS#3. 검수 기준과 호출 방식]]
- [[반복 결함 카탈로그]]: 적대 발췌 호출 시 동봉
