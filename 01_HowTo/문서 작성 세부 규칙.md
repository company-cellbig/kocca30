---
title: 문서 작성 세부 규칙
type: howto
status: stable
tags: [howto, convention, 문체, 구조]
created: 2026-08-21
updated: 2026-08-21
---

> 문서 구조화의 세부 규칙임: 유형별 기본 구조, 구조화 규칙, 금지, 목록 형식 선택(라벨형과 중첩 불릿). [[CONVENTIONS#3.8 구조 우선 작성 (정보 설계)]]의 상세 정본임. 핵심 원칙은 CONVENTIONS가, 셀프체크는 [[AGENTS|에이전트 작업 지침]]이 소유하고 이 문서를 가리킴.

# 1. 문서 유형별 기본 구조

문서 성격에 맞게 택일하고 가감함.

- 개념 설명: 개요 → 핵심 개념 → 세부 항목 → 예시 → 주의 → 요약
- 절차 안내: 목적 → 전제 → 단계별 절차 → 확인 방법 → 문제 해결
- 비교: 비교 기준 → 항목별 비교표 → 장단점 → 선택 기준 → 권장안
- 정책과 규칙: 적용 범위 → 원칙 → 세부 규칙 → 예외 → 체크리스트

# 2. 구조화 규칙

- 한 문단 한 주제. 4줄 이상으로 길어지면 목록, 표, 하위 헤더, sub-bullet로 분리. 라벨에 서너 줄을 1단계 불릿로 욱여넣지 않고, 라벨을 부모로 올려 내용을 sub-bullet로 ([[반복 결함 카탈로그]] STRUCT-007)
- 같은 성격은 같은 항목 아래 묶고, 다른 성격은 한 묶음에 섞지 않음
- 같은 수준의 항목은 같은 문법 형태로 맞춤 (병렬 구조). 예: 한 묶음을 모두 "라벨: 값" 또는 모두 "~함"으로 통일
- 추상 설명 뒤에는 가능하면 예시를 붙임. 실행할 내용은 체크리스트나 번호 단계로, 비교는 표로
- 중요도, 순서, 의존관계가 있으면 명시

# 3. 금지

- 긴 문장을 여러 개 이어붙여 한 덩어리로 적기
- 헤더 없이 문단만 나열하기
- "또한", "그리고", "이와 함께"만 반복하며 잇기
- 원문 순서에 끌려가기 (독자가 이해하기 좋은 순서로 재배열)
- 모호한 항목명("내용", "기타", "참고")만 반복하기

# 4. 목록 형식 선택과 라벨형, 중첩 불릿 규칙

불릿은 문장 나열 도구가 아니라 정보 구조화 도구임. 내용 성격에 맞는 목록 형식을 고르고, 라벨형과 중첩 불릿에 아래 규칙을 적용함.

**가) 목록 형식 선택**

| 내용 성격 | 형식 |
|---|---|
| 순서 무관한 병렬, 단순 목록이나 요약, 짧은 속성, 빠른 확인 | 일반 불릿 |
| 항목마다 짧은 설명(라벨: 내용으로 끝남) | 라벨형 불릿 (`- **라벨**: 설명`) |
| 상위 항목의 세부 설명, 조건/예외/예시/절차, 담당자별로 작업이 갈림, 입력/출력/저장을 함께, 한 줄로 길어지는 규칙 | 중첩(서브) 불릿 (2단계) |
| 순서, 절차, 우선순위, 플로우 | 번호 목록 |
| 같은 기준으로 여러 항목 비교, 조건과 결과 연결, 상태값/데이터 필드/리소스/예외 정의 (값이 짧고 균일할 때) | 표 |

표는 값이 짧고 균일할 때만 적합함. 값이 길거나 들쭉날쭉하면(예: 예외 처리 설명) 셀이 과적되니 라벨형이나 서브 불릿을 씀. 여러 형식이 걸치면 우선순위로 판정함: 값이 짧고 균일하면 표, 항목마다 설명이 길거나 하위가 있으면 서브 불릿, 순서가 핵심이면 번호 목록, 단순 동등 나열이면 일반 불릿.

**나) 라벨형 불릿 규칙**

- 라벨은 명사형으로, 한 묶음 안에서 같은 추상 수준으로 맞춤
- 콜론은 볼드 밖에 둠: `**라벨**: 설명` (`**라벨:**` 아님)
- 설명은 1문장 이내가 원칙. 1문장을 넘기거나 항목이 3개 이상이면 중첩(2단계)으로 분리 ([[#2. 구조화 규칙]])
- 한 묶음의 항목별 설명 길이를 비슷하게 유지
- 라벨이 모호하면 더 구체적인 이름으로. "기타", "내용", "참고" 같은 포괄 라벨은 지양 ([[#3. 금지]])

**다) 중첩 불릿 규칙**

- 중첩은 상위와 하위 관계가 있을 때, 긴 목록을 범주로 묶을 때, 한 항목에 하위 요소가 필요할 때만 씀
- 최대 2단계. 3단계 이상 필요하면 제목과 소제목(헤더)으로 분리 ([[CONVENTIONS#4. 헤딩 / 넘버링]])
- 상위는 범주명, 하위는 세부 내용. 같은 단계는 같은 문법 형태로 맞춤(병렬)
- 하위 항목이 1개뿐이면 중첩하지 말고 상위 문장에 포함 ([[반복 결함 카탈로그]] STRUCT-004). 단, 형제 라벨 묶음이 병렬이고 그중 일부가 다중 하위를 가질 때는, 시각 일관성을 위해 단일 하위 라벨도 중첩을 허용함(항목별 최적화보다 묶음 병렬성 우선). 이 허용은 재료가 있는 항목에 한하며, 병렬을 채우려고 재료 없는 항목을 만들지 않음 ([[CONVENTIONS#1. 핵심 원칙]] 7)

# 5. 관련 문서

- [[CONVENTIONS|위키 작성 규칙]]: 핵심 원칙과 문체 기본. 본 문서의 상위 정본
- [[반복 결함 카탈로그]]: 구조 결함(STRUCT 계열)의 재발 사례 누적처
