# LLM Wiki — Personal Knowledge Repository

Karpathy의 LLM-Wiki 방식을 따르는 개인 지식 저장소.
LLM 에이전트(Claude Code 작성, Codex 검수)가 직접 마크다운 문서를 작성/편집/연결/검수하여 지식을 축적한다.

## 핵심 원칙

1. **에이전트가 위키 운영자** — 사용자가 주제를 던지면 Claude Code가 문서를 생성/갱신하고, Codex가 검수한다.
2. **Atomic Notes** — 하나의 파일에 하나의 주제. 파일명이 곧 제목.
3. **Wikilinks** — 문서 간 연결은 `[[문서명]]` 형식 (Obsidian 호환).
4. **점진적 축적** — 한 번에 완벽한 문서를 만들지 않고, 대화를 거듭하며 보강한다.
5. **한국어 우선** — 문서는 한국어로 작성. 기술 용어는 영문 병기 가능.

## 디렉토리 구조

```
/                     # vault root
├── AGENTS.md         # 이 파일 — 에이전트 작업 지침 (Claude/Codex 공용)
├── CLAUDE.md         # AGENTS.md 호환용 stub
├── 00_Index/         # 목차, 지도 문서 (MOC: Map of Content)
├── 01_Concepts/      # 개념 정리 (정의, 원리, 이론)
├── 02_HowTo/         # 실용 가이드, 레시피, 튜토리얼
├── 03_References/    # 논문 요약, 링크, 인용
├── 04_Projects/      # 프로젝트별 노트
├── 05_Logs/          # 학습 일지, 실험 기록
└── assets/           # 이미지, 첨부파일
```

## 문서 작성 규칙

### 파일명
- 공백 대신 언더스코어(`_`) 사용 금지 — Obsidian은 공백 허용
- 한국어 파일명 사용 가능 (예: `트랜스포머.md`)
- 영문 약어가 보편적인 경우 영문 사용 (예: `LLM.md`, `RAG.md`)

### 프론트매터 (YAML)
모든 문서 상단에 다음 메타데이터를 포함한다:

```yaml
---
title: 문서 제목
tags: [태그1, 태그2]
created: YYYY-MM-DD
updated: YYYY-MM-DD
---
```

### 본문 구조
```markdown
# 제목

> 한 줄 요약 (TL;DR)

## 핵심 내용
- ...

## 세부 설명
- ...

## 관련 문서
- [[관련문서1]]
- [[관련문서2]]

## 참고
- 출처, URL 등
```

### 문체
- **두괄식** — 결론, 핵심을 먼저 쓰고, 부연은 뒤에 배치
- **개조식** — 불릿 포인트 중심으로 간결하게 서술
- **음슴체** — "~이다", "~한다" 대신 "~임", "~함" 체를 사용
- 한 문장은 짧게, 불필요한 수식어 제거
- 표, 리스트를 적극 활용하여 가독성 확보
- **가운뎃점(`·`) 사용 금지** — 항목 나열은 쉼표(`,`) 또는 슬래시(`/`)로 대체

### Wikilink 규칙
- 새 개념이 등장하면 `[[개념명]]`으로 링크를 건다.
- 링크 대상 문서가 아직 없어도 괜찮다 (나중에 생성).
- 별칭이 필요하면 `[[파일명|표시텍스트]]` 형식 사용.

### 수치/목표 인용 원칙 (엄격)

문서에 등장하는 모든 **구체적 수치**와 **목표**는 반드시 `03_References/` 하위 원문을 직접 참조하여 작성한다. 임의 추정/창작은 금지.

**대상 항목**
- 정량 지표: 예산, 인원, 기간, 페이지 수, 비율(%), 점수, KPI, TRL 등
- 목표 진술: 사업 목표, 성과 목표, 정량/정성 목표, 핵심 성과지표
- 일정/기한: 착수일, 종료일, 단계별 마일스톤, 마감 일자
- 조직 정보: 컨소시엄 구성, 역할 분담, 기관별 담당 비율
- 산출물 명세: 결과물 수량, 규격, 인도 조건

**작성 절차**
1. 수치/목표를 적기 전에 `03_References/` 하위 원문(특히 `_locked/RFP.md`, `_locked/컨소시엄 별 역할.md`)을 먼저 확인
2. 문서 내 인용 시 출처를 wikilink로 명시 — 예: `[[RFP]] §3.2`, `[[컨소시엄 별 역할#수행기관]]`
3. 원문에 해당 정보가 없으면 **추정 금지** — `원문 미명시` 또는 `확인 필요` 로 표기
4. 원문 간 수치가 충돌하면 양쪽을 모두 표기하고 사용자에게 확인 요청
5. 외부 자료(웹, 일반론)에서 가져온 수치는 과제 고유 수치와 명확히 구분 표기 (출처 URL 포함)

**금지 사항**
- 그럴듯해 보인다는 이유로 임의 수치 제시 금지
- 시장 평균치/업계 통상치를 과제 고유 수치인 양 기재 금지
- 출처 없이 "약 N억 원", "N개월", "N명 규모" 같은 어림값 작성 금지
- 원문 수치를 단위 변환/반올림하여 적을 때도 원문 표기를 함께 병기

## 에이전트 작업 지침

### 역할 분담

| 역할 | 담당 | 도구 |
|---|---|---|
| 작성/갱신 | Claude Code | Read, Edit, Write, Grep, Glob |
| 검수/lint | Codex | `/codex:review`, `/codex:adversarial-review` |
| 중재 | 사용자 | 충돌 해결, 최종 승인 |

### 문서 생성 시
1. 적절한 디렉토리에 파일 생성
2. 프론트매터 포함
3. 기존 관련 문서에 wikilink 추가 (양방향 연결)
4. MOC(00_Index) 문서에 새 항목 등록

### 문서 갱신 시
1. 기존 내용을 먼저 읽고 파악
2. `updated` 날짜 갱신
3. 기존 구조를 존중하며 내용 추가/수정
4. 새로운 연결이 생기면 wikilink 추가

### 작업 사이클

```
[Claude Code] 작성/갱신 → log.md 기록 → git commit
                           ↓
[Codex]       /codex:review 또는 /codex:adversarial-review
                           ↓
[사용자]      검수 결과 검토 → 수용/반영 → 다음 사이클
```

### 로그 기록 (log.md)
모든 작업 후 `05_Logs/log.md`에 다음 형식으로 항목을 추가한다:

```markdown
## [YYYY-MM-DD] {작업유형} | {제목 또는 주제}

- 작업 내용 요약
- 생성/수정된 파일 목록
- 새로 생성된 wikilink
```

**작업유형 접두사**
- `ingest` — 새 소스(기사, 논문, URL 등)를 읽고 위키에 통합
- `query` — 위키를 대상으로 질문하고 답변을 생성
- `create` — 새 문서 생성
- `update` — 기존 문서 수정
- `lint` — 위키 상태 점검 (모순, 고아 페이지, 누락 링크 등)
- `review` — Codex 검수 결과 반영

**규칙**
- 작업이 끝날 때마다 반드시 log.md에 기록한다
- log.md가 없으면 먼저 생성한다
- 로그는 최신 항목이 **위**에 오도록 역순으로 추가한다
- Unix 도구로 파싱 가능하도록 `## [날짜]` 형식을 정확히 지킨다

### 수정 금지 문서 (read-only)

`03_References/_locked/` 하위 문서는 외부 원본 또는 확정본으로, 에이전트가 **읽기 전용**으로 취급한다.

- 내용 변경, 재작성, 파일 이동/삭제 금지
- 읽기(Read)와 인용은 허용
- 수정이 필요하다고 판단되면 사용자에게 먼저 확인하고 승인받은 후에만 작업한다

현재 락 대상:
- `03_References/_locked/RFP.md` — 과제 RFP 원문
- `03_References/_locked/컨소시엄 별 역할.md` — 컨소시엄 확정본

### PDF 읽기 원칙

PDF 자료(RFP, 보고서, 논문 등)를 분석할 때는 다음 순서로 처리한다.

1. **텍스트 추출 우선** — 텍스트 레이어가 있는 PDF는 `pdftotext -layout`로 추출하여 표/들여쓰기/계층 목차를 보존한 텍스트 파일로 변환 후 읽음
2. **스캔본은 이미지 변환** — 텍스트 추출이 비거나 깨지면 스캔본으로 판단, `pdftoppm -png`로 페이지별 PNG로 변환 후 멀티모달로 읽음
3. **Read 도구 직접 열기는 최후 수단** — 20페이지 제한, 표 정확도 저하, 토큰 비용 문제 때문에 위 두 방법이 불가능할 때만 사용

환경 의존적인 실행 경로(poppler 바이너리 절대경로 등)는 사용자 로컬 메모리에 저장되어 있다.

## Codex 검수 기준 (lint checklist)

문서 변경(커밋) 직후 `/codex:review` 또는 `/codex:adversarial-review`가 다음 항목을 점검한다.

### 1. 수치/목표 인용 원칙 위반
- 정량 지표(예산, 인원, 기간, %, KPI, TRL 등)에 출처 wikilink가 동반되는가
- `[[RFP]]`, `[[컨소시엄 별 역할]]` 등 원문 출처 누락 검출
- 추정/어림값(`약 N억`, `N개월 규모`)이 출처 없이 등장하지 않는가

### 2. 수정 금지 영역 무단 변경
- `03_References/_locked/` 하위 파일이 사용자 승인 없이 수정되었는가
- 락 파일의 헤딩 구조(예: `# 5-1` ~ `# 5-3`)가 변경되었는가

### 3. 문체 규칙
- 음슴체 준수 ("~이다", "~한다" → "~임", "~함")
- 두괄식, 개조식 구조 유지
- 가운뎃점(`·`) 사용 여부 (금지)
- 불필요한 수식어, 길어진 문장 검출

### 4. 위키 정합성
- 깨진 wikilink (대상 파일 부재)
- 신규 문서가 `00_Index/MOC.md`에 등록되었는가
- 양방향 연결 누락 (A → B만 있고 B → A 없음)

### 5. 메타데이터
- 프론트매터 `updated` 필드가 실제 수정 시각과 일치하는가
- `tags` 누락 또는 중복

### 6. 작업 로그
- `05_Logs/log.md`에 변경분이 기록되었는가
- 최신 항목이 맨 위에 위치하는가
- `## [YYYY-MM-DD] {유형} | {주제}` 형식 준수

### 검수 호출 예시

```
/codex:adversarial-review --background CLAUDE/AGENTS.md "Codex 검수 기준 (lint checklist)" 6개 항목 전수 점검
```

검수 결과 반영은 `review` 작업유형으로 별도 커밋한다.

## 하지 말 것
- 사용자가 요청하지 않은 문서를 임의로 삭제하지 않는다
- 확실하지 않은 정보를 사실처럼 적지 않는다 — 불확실하면 명시
- 영어로 작성하지 않는다 (기술 용어 병기는 허용)
- assets 폴더 외 위치에 바이너리 파일을 두지 않는다
- log.md 기록을 생략하지 않는다
- `03_References/_locked/` 하위 문서를 임의로 수정하지 않는다
- Codex 검수 결과를 사용자 확인 없이 자동 반영하지 않는다
