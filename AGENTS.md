---
title: 에이전트 작업 지침
type: reference
status: stable
tags: [meta, agent]
created: 2026-05-08
updated: 2026-09-11
---

@CONVENTIONS.md

> `@import`는 Claude Code 전용 기능임. Codex 등 import를 자동으로 펼치지 않는 도구는 작업 전에 아래 규칙 로딩 순서에 따라 필요한 문서를 직접 끝까지 읽어야 함.

AI 에이전트가 작성자, 외부 검토자와 중재자로 역할을 나눠 마크다운 문서를 작성, 편집, 연결하고 검수하는 운영 규칙임. 위키 문서의 형식과 문체, 링크, 근거 및 로그 양식은 [[CONVENTIONS|위키 작성 규칙]]이 소유함.

# 1. 핵심 원칙

## 1.1 규칙 로딩 순서

행동하기 전에 다음 순서로 지침을 읽음. 일부만 읽었거나 출력이 잘렸으면 나누어 읽더라도 문서 끝까지 확인함.

1. 이 `AGENTS.md` 전체를 읽음
2. 위키 문서를 작성, 갱신 또는 검수하면 [[CONVENTIONS|위키 작성 규칙]] 전체를 직접 읽음
3. 아래 선택표에서 해당하는 모든 행의 문서를 추가로 읽음. 2단계에서 이미 읽은 문서는 반복하지 않아도 됨
4. 이 문서의 수정 금지 영역, 사용자 승인 게이트와 자동 검사 의무를 확인한 뒤 작업함

- `CLAUDE.md`의 `@import`를 Codex의 로딩 수단으로 간주하지 않음
- 하위 폴더의 `AGENTS.md`가 저장소 루트에서 시작한 세션에 나중에 자동 추가된다고 가정하지 않음
- 루트부터 현재 작업 디렉터리까지 자동 합산되는 지침 크기를 `project_doc_max_bytes` 설정과 대조함
- 기본값과 탐색 순서는 [OpenAI 공식 AGENTS.md 안내](https://learn.chatgpt.com/docs/agent-configuration/agents-md)를 기준으로 확인함

## 1.2 작업별 필수 문서 선택표

| 작업 조건 | 반드시 읽을 문서 |
|---|---|
| 위키 문서 생성, 갱신 또는 검수 | [[CONVENTIONS|위키 작성 규칙]] |
| 본문 구조와 개조식 설계 | [[문서 작성 세부 규칙]] |
| HWPX 또는 DOCX 변환과 수집 | [[docx 변환 파이프라인]] |
| PDF 분석 | [[PDF 읽기 절차]] |
| 자기검수와 같은 결함류 전수 점검 | [[반복 결함 카탈로그]] |
| 외부 검수 패킷 작성 | [[외부 검수 포커스]] |
| 위키 문서 생성, 갱신, 수집, 질의 또는 유지 | [[위키 운영 워크플로]] |
| 인포그래픽 제작 | [[인포그래픽 생성 지침]] |
| 과거 절 참조 오변환 복구 | [[절 참조 오변환 점검 절차]] |
| 클라이언트 데이터 번역 | [[클라이언트 데이터 번역 규칙]] |

## 1.3 역할 분리의 원칙

- **작성자 단일화**: Codex가 초안, 자기검수, 사용자 승인 항목 반영, MOC, 로그와 커밋을 한 흐름에서 수행함
- **외부 검토자 단일화**: Claude Code가 독립 세션에서 지정된 범위의 발견만 보고하고 파일은 수정하지 않음
- **맥락 격리**: 외부 검토 세션에는 완성된 변경과 검토 포커스 및 원자료만 전달하고 작성 의도와 중간 추론은 전달하지 않음
- **도구 분리**: 작성 도구와 외부 검토 도구를 달리해 같은 모델의 표현 습관과 누락이 검토에 반복될 가능성을 낮춤
- **라우터 경계**: 이 문서는 운영 권한, 규칙 로딩 순서와 호출 조건을 소유하며 선택표의 정본 문서가 소유한 세부 절차나 검사 목록을 옮겨 적지 않음

역할별 책임은 [[#2.1 역할 분담]], 사용자 중재 범위는 [[#2.4 사용자 승인 게이트]]가 정함.

# 2. 에이전트 작업 지침

## 2.1 역할 분담

| 역할 | 도구 | 책임 |
|---|---|---|
| 작성자 | Codex | 초안과 갱신, P1과 P5 자기검수, 자동 검사, MOC와 로그, 커밋, 승인 항목 반영 |
| 외부 검토자 | Claude Code | 새 독립 세션에서 사실과 정합, 적대 및 가독성 중 지정된 범위를 검토하고 발견만 보고 |
| 중재자 | 사용자 | 자기검수와 외부 검토 발견을 수용, 보류 또는 반려 |

## 2.2 역할별 호출 절차

### 2.2.1 작성자

작성자는 한 사이클에서 아래 순서를 따름.

1. **근거 추출과 작성**
	- 원자료의 개수, 분류, 단계나 목차를 도출하면 분석 범위와 분절 기준을 먼저 기록함
	- 범위를 끝까지 읽고 주체, 행동, 상태와 관계가 바뀌는 근거 단위를 위치와 함께 제한 없이 추출함
	- 최초 추출표를 저장한 뒤 임시 ID, 합치기와 나누기 이력, `근거 단위 → 새 구조` 대응표를 남기고 구조화함
	- 원문에 없는 내용은 `설계 추가`, 쓰지 않은 단위는 `제외`와 사유로 표시함
	- 원자료 분석이 아니면 [[CONVENTIONS#3.8 구조 우선 작성 (정보 설계)]]과 [[문서 작성 세부 규칙]]에 따라 바로 구조를 설계함
	- 문서 생성과 갱신의 세부 순서는 [[위키 운영 워크플로#5. 공통 문서 생성과 갱신 절차]]를 따름. 본문, MOC와 로그는 그 절차에서 요구하는 시점에 갱신함
2. **자동 정비와 검사**
	- 모든 사이클에서 `node scripts/wiki_number.mjs --write`, `node scripts/wiki_lint.mjs`, `node scripts/quote_check.mjs`를 차례로 실행함
	- 넘버링 어긋남과 lint 오류 및 인용 어긋남이 각각 0건인지 확인함
	- 경고와 건너뜀은 대상 커밋이 있으면 그 부모 커밋과 비교함. 커밋 전이면 작업 시작 전에 측정한 값과 비교해 새로 생긴 것인지 구별함
	- 헤딩 번호는 손으로 붙이지 않고 `wiki_number.mjs`에 맡김. 중복 헤딩으로 anchor 갱신이 보류되면 사람이 확정함
	- `git config core.hooksPath scripts/hooks`로 커밋 훅을 사용함. 훅은 실행 가능한 경우 스테이징된 내용을 대상으로 세 자동 검사와 변경한 마크다운의 `updated` 날짜를 검사하고, 어긋나면 커밋을 막음
	- Node가 없어 훅 검사를 건너뛰면 Node 실행 환경을 복구한 뒤 검사함. 그 밖의 이유로 훅이 검사를 건너뛰면 스테이징된 내용을 대상으로 세 자동 검사와 `updated` 날짜 검사를 직접 수행함
	- 커밋 때 `scripts/diff_companion.mjs`가 내는 비차단 알림이 짚은 절은 [[위키 운영 워크플로#5.2 문서 갱신 절차]] 5번에 따라 다시 확인함
3. **자기검수와 독립 대조**
	- P1 사실과 출처 및 P5 위키 정합성을 인라인으로 보고함. anchor는 추정하지 않고 `rg`로 실제 헤딩과 사용처를 전수 확인함
	- 정본으로 남길 수치와 목록은 채팅에만 쓰지 않고 파일에 기록해 자동 검사와 후속 대조 범위에 넣음
	- 오타, 링크, 문법과 형식의 작은 결함은 즉시 정정할 수 있음. 사실 주장의 내용이나 단정 수준, 정책과 요청 범위 밖 확장은 G1에서 사용자가 결정함
	- 작은 변경으로 새로 쓰거나 고친 정본 문장은 G1 전에 새 Codex 서브에이전트가 독립 가독성 검토를 1회 수행함. 완성 텍스트와 문법, 명료성, 중복 및 구조 점검 지시만 주고 작성 의도와 중간 추론은 주지 않음
	- 큰 변경과 외부 노출 정본은 G1 뒤 Claude Code 검수 패킷에 가독성 발췌를 포함함
	- 사용자가 Claude Code 검토를 면제하거나 거부하면 새 Codex 서브에이전트가 새로 쓰거나 고친 모든 정본 문장의 문법, 명료성, 중복과 구조를 독립 검토함
	- Claude Code를 사용할 수 없어 외부 검토 전체를 대체하면 [[#2.2.2 외부 검토자]]의 대체 절차를 따름
	- 사실 층 변경은 G1 전에 새 Codex 서브에이전트가 사실 대조를 한 차례 더 수행함. 완성 본문과 원자료 경로만 주고 각 주장에 근거 위치를 제시하게 하며, 근거가 없으면 `없음`으로 남김
	- 독립 검토는 수행 사실과 발견 및 반영 건수만 보고함. 무발견을 결함 부재의 증명이나 `검증 완료`로 표현하지 않음
	- 인용은 쓸 때마다 원문을 다시 열어 문자, 위치, 주체와 인과, 추가 서술, 부분과 전체 및 앞뒤 맥락을 대조함. 상세 기준은 [[CONVENTIONS#3.6 수치/목표 인용 원칙 (엄격)]]을 따름
	- 같은 결함류를 전수 점검하고 [[반복 결함 카탈로그]]의 관련 항목을 대조함
4. **커밋과 승인 반영**
	- 사용자 직접 요청 범위와 즉시 정정 가능한 작은 결함은 G1 전에 커밋할 수 있음
	- G2 뒤에는 사용자가 수용한 외부 검토 항목만 반영하고 `review` 로그와 커밋을 남김
	- 자동 검사 뒤 파일을 다시 고쳤으면 세 자동 명령을 다시 실행함
	- G1 전 수정이나 G1에서 즉시 정정하기로 한 항목을 외부 검토 전에 반영했을 때
		- 영향 범위의 자기검수와 해당 독립 대조를 반복함
		- 새 발견이 나오면 G1 판정으로 돌아감
	- G2 수용 항목을 반영했을 때
		- 자동 검사와 영향 범위의 P1 및 P5 자기검수를 반복함
		- 독립 가독성 재검수 대상은 사용자가 수용한 발견을 반영하면서 본문을 고친 파일의 변경 범위로 한정함
		- 독립 검토 지시에는 대상 파일 경로와 diff 범위를 명시함
		- 검토자는 지시에 명시된 범위 안에서만 이번 재검수의 새 발견을 보고함
		- `99_Logs/`의 로그 문서와 외부 검수 결과 문서는 독립 가독성 재검수에서 제외함
		- 작성자는 로그와 외부 검수 결과 문서에 기록된 사용자 판정, 발견 건수, 자동 검사 수치, 수정 파일과 관련 커밋 목록이 실제 G2 처리 결과와 일치하는지만 대조함
		- 사실 층을 바꿨으면 사실 대조도 반복함
		- 새 발견이 나오면 사용자 판정을 다시 요청한 뒤 커밋함

완료 조건은 변경 파일 커밋과 자동 검사 오류 0건임. 새 위키 문서 작성이나 이동처럼 해당하는 작업은 MOC 등록과 wikilink도 확인함.

### 2.2.2 외부 검토자

- **진입 조건**: G1에서 외부 검토 진행이 결정됨
- **검수 패킷**: 구성과 발췌 정본은 [[외부 검수 포커스#5. 검수 패킷 운용 규칙]]임
- **호출**: 사용자가 패킷을 새 Claude Code 세션에 전달함. Codex가 Claude Code를 직접 호출할 수 있다고 가정하지 않음
- **권한**: Claude Code는 새 세션에서 저장소와 지정 원자료를 읽고 발견만 보고함. 본문, 로그와 Git을 수정하거나 승인 판정을 내리지 않음
- **대체 절차**: Claude Code를 쓸 수 없으면 새 Codex 스레드에 같은 패킷을 주고 `동일 모델 독립 맥락 대체 검토`라고 표시함. 외부 노출 정본, 정책 변경 또는 사실 주장이 많은 변경은 사용자의 명시적 외부 검토 면제가 있어야 이 대체만으로 끝낼 수 있음

### 2.2.3 중재자

- 자기검수와 외부 검토 발견을 항목별로 수용, 보류 또는 반려함
- 작성자가 이미 즉시 정정한 작은 결함을 사후 확인함
- `근거 약함` 발견은 [[CONVENTIONS#3.7 외부 근거의 권위 계층]]의 L1~L5 등급을 확인한 뒤 결정함

## 2.3 작업 사이클

```text
Codex 작성과 자기검수, 로그 및 커밋
  → G1 사용자 결정
  → 필요한 경우 Claude Code 외부 검토
  → G2 사용자 결정
  → Codex가 수용 항목만 반영, 로그 및 커밋
```

사이클의 범위와 분할 조건은 다음과 같음.

- 한 작성 사이클은 요청된 변경의 작성과 자기검수부터 G1, 필요한 외부 검토, G2와 수용 항목 반영까지 이어짐
- 작성과 자기검수 및 승인 뒤 반영은 같은 Codex 작성자 흐름에서 수행함
- 계획이 여러 작성 단계를 따로 정한 경우에만 각 단계를 별도 사이클로 진행함. 앞 단계의 커밋과 게이트가 끝나기 전에 다음 단계로 넘어가지 않음
- G1, 읽기 전용 외부 검토와 G2 자체는 별도 작성 사이클이나 커밋을 요구하지 않음
- 파일 변경이 없는 단순 조회만 수행한 경우는 작성 사이클로 보지 않음. 진입 판정은 [[#2.5 로그 기록 의무]]를 따름

## 2.4 사용자 승인 게이트

### 2.4.1 G1: 작성자 자기검수 후

- **입력**: 자동 검사 결과, 자기검수 발견 목록, G1 전에 즉시 정정하고 커밋한 항목과 반영 건수임. 발견이 없으면 무발견이라고 보고함
- **사용자 직접 검토**: 자동 통과 조건을 충족하지 못하면 사용자가 작성자 보고와 변경을 보고 추가 결함을 적극 탐색함
- **사용자 결정**: 자기검수와 사용자 직접 검토에서 나온 각 발견을 즉시 정정, 외부 검토 전달 또는 반려하고, 외부 검토 진행이나 생략을 함께 결정함
- **자동 통과**: G1 점검은 항상 수행함. 자동 검사 오류 0건, 자기검수 무발견과 [[#2.4.3 작은 변경 사이클 면제 (엄격)]] 조건을 모두 충족하면 로그에 자동 통과로 기록하고 사용자 응답 대기만 생략함. 자기검수 무발견은 결함 부재의 증명이 아니라 통과의 필요조건임. 사용자가 나중에 결함을 발견하면 새 사이클에서 정정함
- **판정 전환**: 조건 하나라도 충족하지 않으면 사용자 판정을 기다림

### 2.4.2 G2: 외부 검토 후

- **수용**: 작성자가 해당 항목만 본문에 반영하고 `review` 로그와 커밋을 남김
- **보류**: 로그에 잔류 사유를 기록하고 다음 사이클에서 다시 논의함
- **반려**: 로그에 기각 사유를 기록함
- 사용자 명시 결정 전에는 어떤 외부 검토 발견도 본문에 반영하지 않음
- 발견이 0건이면 반영 항목이 없음을 보고하고 G2를 끝냄
- 같은 결함 유형이 두 번째로 재발하면 [[반복 결함 카탈로그]]에 등재함. 검토 관점 자체가 부족해 반복됐다면 [[외부 검수 포커스]]의 해당 점검 항목도 보강함

### 2.4.3 작은 변경 사이클 면제 (엄격)

면제 대상은 Claude Code 외부 검토뿐임. 자동 검사, 자기검수, 독립 가독성 검토와 G1 판정은 생략하지 않음.

- **면제 가능**: 의미를 바꾸지 않는 frontmatter만 수정하거나, 오타 또는 명백히 낡은 표현 한 줄만 고치는 경우. frontmatter 변경은 물리적 줄 수보다 의미 변경 여부로 판정함
- **면제 불가**: 새 절이나 단락, 표 구조, 다수 줄의 본문, 정책, 새 문서 또는 헤딩을 바꾸는 경우
- **면제 불가**: 외부 노출 산출물을 바꾸거나 정량 인용 또는 사실 주장이 많은 경우
- 두 조건이 겹치면 면제 불가를 우선함
- 변경이 면제 가능한지 애매하면 면제 불가로 보고 G1에서 외부 검토를 제안함
- 면제하면 로그에 `(cycle-exempt)`와 사유를 기록함

## 2.5 로그 기록 의무

작성자의 대화와 조회를 모두 기록하지는 않음. 다음 작업은 작업 시점에 해당하는 `99_Logs/YYYY/YYYY년 M월 작업 로그.md`에 기록함.

- **보존할 파일 변경**: 작업 결과로 보존할 저장소 파일을 생성, 수정, 이동 또는 삭제한 작업. Git 추적 여부와 관계없이 적용하되 임시 파일, 캐시와 재생성할 중간 결과는 제외함
- **검수와 게이트**: 사용자가 요청한 검수 결과, 외부 검토 결과, G1과 G2 판정을 기록함
- **검수와 게이트의 포함 범위**: 발견 0건, 자동 통과, 수용, 보류, 반려, 작은 변경 사이클 면제와 외부 검토 면제를 포함함
- **파일 변경이 없는 질의**: 결과가 다음 세 조건을 모두 충족할 때만 기록함
	1. **새로움**: 기존 문서와 로그에 없는 합성이나 판단임
	2. **지속성**: 현재 대화가 끝난 뒤에도 다시 사용할 가치가 있음
	3. **행동 영향**: 후속 작업의 범위, 정책, 수치, 우선순위, 절차 또는 해석을 바꿈
- **질의 결과 우선**: 기존 정보의 위치나 상태 확인, 기존 문서의 재진술과 설명, 일시적인 명령 결과 및 앞선 답변의 뜻을 확인하는 질문은 원칙적으로 기록하지 않음. 다만 그 과정에서 위 세 조건을 모두 충족하는 결론이 나오면 질문의 시작 유형과 관계없이 기록함
- **애매한 경우**: 세 조건 충족 여부가 불분명하면 기록하지 않음
- **사용자 지정**: 파일 변경이 없는 질의는 사용자가 기록 또는 비기록을 명시하면 그 지시를 우선함
- **사용자 지정의 적용 제외**: 보존할 파일 변경 및 검수와 게이트를 기록할 의무와 작성 사이클의 자동 검사 의무에는 적용하지 않음
- 같은 주제의 연속 질의는 질문마다 나누지 않고, 기록 기준을 충족하는 결론이 확정됐을 때 작업 단위 하나로 묶음

외부 검토자는 로그를 수정하지 않고, 작성자가 G2 결정 뒤 외부 검토 결과와 처리를 기록함. 해당 월의 로그가 없으면 먼저 만들고 `99_Logs/log.md` 색인에 연결하며 [[CONVENTIONS#7. 로그 항목 양식]]을 따름.

## 2.6 수정 금지 영역 준수

`_locked/`, `_sources/`, `_figures/`, `_reviews/`와 `_archive/`를 포함한 `_xxx/` 접두 폴더는 기본적으로 사용자 승인 없이 수정하지 않음. 앞으로 생기는 `_xxx/` 폴더에도 자동 적용하며 예외와 자세한 범위는 [[CONVENTIONS#6. 수정 금지 영역]]을 따름.

## 2.7 PDF 읽기 원칙

PDF를 분석할 때는 [[PDF 읽기 절차]]를 끝까지 읽고 따름.

## 2.8 위키 문서의 생성, 갱신과 운영

위키 문서의 생성, 갱신, 수집, 질의, 유지 절차는 [[위키 운영 워크플로]]가 소유함. 본문 변경이 생기면 다른 작성 작업과 같은 게이트를 거치며 로그 유형은 `ingest`, `query`, `create`, `update`, `lint` 또는 `maint`를 사용함. 단순 조회의 기록과 작성 사이클 진입 여부는 [[#2.5 로그 기록 의무]]를 따름.

## 2.9 산출물층 개입 절차 (05_산출물)

`05_산출물/`은 사용자가 직접 쓰는 외부 원고이며 위키 작성 규칙을 적용하지 않음.

1. 사용자가 요청하면 에이전트가 초안을 작성함
2. 사용자가 집필하는 동안 에이전트는 손대지 않음
3. 사용자가 요청하면 에이전트가 검수하고 발견만 보고함. 외부 검토 참여 여부와 관계없이 G2와 같은 항목별 승인 규칙으로 사용자가 수용한 발견만 반영함

사용자 호출 없이 자율 편집하지 않음. 다른 작업 중 결함을 발견하면 고치지 않고 보고함. 문서명이나 anchor 변경 등으로 깨진 링크를 고치는 기계적 정비만 예외로 하며 로그에 남김. 검수에서는 본문에 wikilink와 임베드가 없는 자족성, 정본과 수치의 일치, 논리와 가독성을 확인함.

- `wiki_number.mjs`는 `05_산출물/`의 헤딩 번호를 바꾸지 않음. 산출물 밖 문서의 anchor가 바뀌어 생기는 산출물 안 링크 정비만 수행함
- `wiki_lint.mjs`는 `05_산출물/`을 위키 전용 검사에서 제외함. 산출물의 자족성, 정본과 수치의 일치, 논리와 가독성은 사용자가 요청한 검수에서 확인함
- 읽기 전용인 `quote_check.mjs`의 원문 대조는 산출물에도 적용할 수 있음

# 3. 검수 기준과 호출 방식

## 3.1 P1~P5 우선순위

| 우선순위 | 검수 대상 | 정본 |
|---|---|---|
| P1 | 사실 오류와 출처 불일치 | [[외부 검수 포커스#2. 표준 포커스 텍스트\|P1 블록]] |
| P2 | 논리적 비약과 문서 간 모순 | [[외부 검수 포커스#2. 표준 포커스 텍스트\|P2 블록]] |
| P3 | 누락된 반론, 한계와 검증되지 않은 단정 | [[외부 검수 포커스#2. 표준 포커스 텍스트\|P3 블록]] |
| P4 | 문체, 구조와 가독성 | [[외부 검수 포커스#2. 표준 포커스 텍스트\|P4 블록]] |
| P5 | 참조가 실제 내용을 가리키는 위키 정합성 | [[외부 검수 포커스#2. 표준 포커스 텍스트\|P5 블록]] |

P 구분은 검토 시선을 나누기 위한 것이며 외부 검토자는 단일 역할임.

## 3.2 외부 검토자 호출

사용자가 외부 검토를 명시하면 그 요청을 G1의 외부 검토 진행 결정으로 간주하며 작은 변경 면제보다 우선함. 그 밖에는 다음 중 하나라도 해당할 때 G1 뒤 Claude Code 외부 검토를 제안함.

- 새 절이나 단락, 표 구조, 다수 줄, 정책, 새 문서 또는 헤딩을 바꾸는 큰 변경
- 기획서, 결정사항, 정본 또는 제안서처럼 외부에 노출되는 산출물 변경
- 정량 인용 또는 사실 주장이 많은 변경

외부 검토 면제 판단은 [[#2.4.3 작은 변경 사이클 면제 (엄격)]], 대체 절차는 [[#2.2.2 외부 검토자]]를 따름. 외부 검토 결과는 [[#2.4.2 G2: 외부 검토 후]]에서 처리함.

호출 조건에 해당하는 변경의 외부 검토를 사용자가 명시적으로 거부하거나 면제하면 작성자가 그 사유를 로그에 기록함.

호출 조건에 해당하는지 애매하면 외부 검토를 제안함.

# 4. 하지 말 것

- 사용자가 요청하지 않은 문서를 삭제하지 않음
- 불확실한 정보를 사실처럼 쓰지 않고 추정 또는 확인 필요로 표시함
- 기록 대상 작업의 로그를 생략하지 않음
- 작성 사이클의 필수 자동 검사를 로그 기록 여부와 관계없이 생략하지 않음
- 수정 금지 영역과 `05_산출물/`을 승인이나 호출 없이 편집하지 않음
- 결과물인 `04_Projects/`와 `05_산출물/`에 에이전트 작업 지침이나 위키 정비 TODO를 섞지 않음
- Claude Code 외부 검토 결과를 G2 사용자 승인 없이 반영하지 않음
- 외부 근거의 L1~L5 권위 등급을 무시하거나 L5 추론을 L1 정본처럼 단정하지 않음
