---
title: 에이전트 작업 지침
tags: [meta, agent]
created: 2026-05-08
updated: 2026-05-18
---

@CONVENTIONS.md

AI 에이전트(Claude Code, Codex)가 다각도 교차검증 워크플로(3역할: 작성자/외부 검토자/중재자)로 마크다운 문서를 작성/편집/연결/검수하는 운영 규칙. 위키 작성 자체의 표준(디렉토리 구조, 문체, 인용 원칙, 로그 양식 등)은 [[CONVENTIONS|위키 작성 규칙]]에서 정의 — 본 문서가 자동 import 함.

# 1. 핵심 원칙

**에이전트가 위키 운영자** — 사용자가 주제를 던지면 3역할(작성자, 외부 검토자, 중재자)이 다각도 교차검증으로 작성/검수하고 사용자가 중재함. 자세한 분담은 §2.가 역할 분담 참조. 위키 작성 자체의 원칙(Atomic Notes, Wikilinks, 점진적 축적, 한국어 우선)은 [[CONVENTIONS#1. 핵심 원칙]] 참조.

**3역할 설계의 정신** — 실제 도구는 2개(Claude Code, Codex). 도구와 1:1 매핑하는 3역할 — 작성자(Claude Code) / 외부 검토자(Codex) / 중재자(사용자).

# 2. 에이전트 작업 지침

## 가. 역할 분담

| 역할 | 도구 | 책임 | 호출 |
|---|---|---|---|
| 작성자 (Writer) | Claude Code | 초안 작성/갱신 + 자기검수(P1/P5 인라인) + 사용자 승인 항목 본문 반영. wikilink/MOC/log 기록 ([[CONVENTIONS#5. 문서 작성/갱신 절차]] 준수) | 기본 대화 |
| 외부 검토자 (External Reviewer) | Codex | 발췌 첨부 호출로 외부 시선 확보. P1/P5(사실/정합) 발췌 또는 P2/P3(적대) 발췌 선택 | `/codex:adversarial-review` + 발췌 종류 첨부 |
| 중재자 (Arbiter) | 사용자 | 모든 검수 발견에 대한 수용/보류/반려 결정. 자동 반영 금지 | 대화 |

**설계 원리**

- **작성자 단일화** — 초안 작성 + 자기검수 + 사용자 승인 후 본문 반영을 같은 Claude Code 안의 단일 흐름으로. 작업 단계는 git history와 log 작업유형(`create`/`update`/`review`)으로 추적
- **외부 검토자 단일화** — Codex 단일 도구. 시선 분리는 발췌 종류로 구현 (§3.나 발췌 종류별 가이드). `/codex:review`는 native 전용으로 focus text 차단 — `/codex:adversarial-review`만 사용
- **외부 검토자는 fresh context 자동** — Codex 호출마다 새 thread 생성. 작성자 컨텍스트 격리 보장됨

## 나. 역할별 호출 절차

### 1) 작성자

- **진입 조건**: 사용자가 작업을 지시하거나 직전 사이클이 사용자 승인으로 종료됨
- **호출**: 기본 대화
- **3단계 흐름**:
	1. **초안 작성/갱신** — 본문 .md 변경 + log 항목(`create`/`update`) + git commit
	2. **자기검수 (P1/P5 인라인 셀프체크)** — 작성 직후 같은 패스에서 진행. 발견 항목 채팅 인라인 보고. **자기검수 중 발견된 작은 결함은 즉시 정정 가능** (별도 게이트 없이). 단정적 정정과 정책 변경은 G1에서 사용자 결정
	3. **사용자 승인 후 반영** — G2에서 사용자가 "수용"으로 표시한 외부 검토 발견 항목을 본문 반영. log 항목(`review`) + git commit
- **준수**: [[CONVENTIONS#5. 문서 작성/갱신 절차]] (디렉토리, 프론트매터, wikilink, MOC 등록 등)
- **셀프체크 체크리스트 (P1/P5 압축판)**:
	- 모든 정량 주장에 출처 wikilink가 동반되는가 ([[CONVENTIONS#바. 수치/목표 인용 원칙 (엄격)]] 준수)
	- 추정/어림값에 "추정" 또는 "확인 필요" 표기가 있는가
	- 신규 문서가 `00_Index/MOC.md`에 등록됐는가
	- 양방향 wikilink 누락이 없는가 (A→B만 있고 B→A 없음)
	- 깨진 wikilink가 없는가 (대상 파일 부재)
	- frontmatter `updated` 필드가 오늘 날짜와 일치하는가
	- 음슴체, 가운뎃점 금지, 두괄식/개조식 ([[CONVENTIONS#라. 문체]]) 준수하는가
	- 헤딩/넘버링 규칙 ([[CONVENTIONS#4. 헤딩 / 넘버링]]) 준수하는가
- **완료 조건**: 작성 단계는 commit + MOC/wikilink 확인. 반영 단계는 사용자 승인 항목 반영 후 commit

### 2) 외부 검토자

- **진입 조건**: G1 통과 후 (작은 변경은 사이클 면제 §라.3 적용 가능)
- **호출**: `/codex:adversarial-review --background <발췌>` + [[codex 검수 포커스]]의 발췌 첨부
- **발췌 종류 (시선 분리)**:
	- **사실/정합 발췌**: 머리말(회의 기조) + P1(사실 오류/출처) + P5(위키 정합성) + 끝맺음
	- **적대 발췌**: 머리말 + P2(논리 비약) + P3(누락 반론/한계 조건) + 끝맺음 + [[반복 결함 카탈로그]] 본문
- **두 종류 호출은 병렬 또는 직렬** — 같은 commit 대상에 둘 다 호출 가능 (큰 변경 시 권장). 작은 변경은 한 종류 또는 생략
- **권한 sandbox 한계 보완**: Codex가 grep/Get-ChildItem 등 권한 차단으로 wikilink anchor 전수 점검을 못 하는 경우, 작성자가 Claude Code 자체 grep 패스(B 패스)로 보완. 위키 정합성 영역(P5)은 작성자 자기검수 + B 패스 조합이 외부 시선보다 효과적인 경우 많음
- **산출**: 검수 보고 (verdict + 발견 항목 리스트)
- **완료 조건**: 보고 수신 후 G2 진입

### 3) 중재자

- **진입 조건**: 자기검수 발견 또는 외부 검토 발견 도착 후
- **역할**: 항목별 수용/보류/반려 결정
- **권한**: 자동 반영 금지. 사용자 명시 승인 없이 본문 반영 안 됨
- **근거 평가**: 발견 항목이 "근거 약함" 플래그를 포함하면 L1~L5 권위 등급([[CONVENTIONS#사. 외부 근거의 권위 계층]]) 중 어느 등급의 근거가 있는지 명시한 뒤 결정

## 다. 작업 사이클

```
작성자: 초안 작성/갱신 → 자기검수(P1/P5 인라인) → log(create/update)/commit
   ↓
G1 사용자 검토 (자기검수 발견 항목 수용/외부 검토 진행 결정. 발견 0건이면 G1 생략)
   ↓ (외부 검토 필요 결정 시)
외부 검토자: /codex:adversarial-review + 사실/정합 발췌 또는 적대 발췌 (병렬 또는 직렬, 둘 다 가능)
   ↓
G2 사용자 항목별 수용/보류/반려 (모든 반영은 명시 승인 필수)
   ↓
작성자: 수용 항목 본문 반영 → log(review)/commit → 다음 사이클
```

**사이클 정신**: 작성자 1회 호출 안에서 작성 + 자기검수 + (사용자 승인 후) 반영. 외부 검토는 큰 변경에만 선택적으로 호출.

## 라. 사용자 승인 게이트

다각도 검증의 모든 본문 반영은 사용자 명시 승인 후. 자동 반영 금지.

### 1) G1 — 작성자 자기검수 후

- **입력**: 작성자가 보고한 자기검수 발견 사항 (채팅 인라인 리스트, 없으면 0건 보고)
- **사용자 결정** — 두 가지 분기 결합:
	- **자기검수 발견 항목별 결정** (있을 때): 항목별 "즉시 정정" / "외부 검토에 넘김" / "반려"
	- **외부 검토 진행 여부** (발견 0건이어도 별도 결정): "외부 검토 진행" / "외부 검토 생략, 사이클 종료" (작은 변경 면제 §3 또는 사용자 판단)
- **반영 경로**: 자기검수 중 작성자가 이미 즉시 정정한 항목은 보고에 명시 (G1은 그 정정에 대한 사후 확인 + 외부 검토 진행 여부 결정)
- **자동 통과 조건**: 자기검수 발견 0건 + 작은 변경 면제 조건 충족 시 G1과 외부 검토 모두 생략, 사이클 자동 종료

### 2) G2 — 외부 검토 후

- **입력**: 외부 검토자 보고서의 발견 항목 (표 형태 권장)
- **사용자 결정**: 항목별
	- **수용** → 작성자가 본문 반영 (log `review` 작업유형)
	- **보류** → log에 잔류 메모로 기록, 다음 사이클에서 재논의
	- **반려** → 기각 사유를 log에 명시
- **반영 권한**: 작성자는 사용자가 "수용"으로 표시한 항목만 손댐. 다른 항목 무단 반영 금지

### 3) 작은 변경 사이클 면제

다음 경우에 한해 외부 검토 생략, 작성자(자기검수 포함)까지만 수행:

- frontmatter only 수정 (`updated` 갱신 등)
- 1행 오타 수정
- 명백한 wikilink/MOC 누락 보정
- 운영 메타 변경 (역할 분담/사이클 규칙 자체 변경 — 외부 검토자가 검수하기 부적합)

면제 적용 시 log 항목에 `(cycle-exempt)` 표기.

## 마. 로그 기록 의무

모든 작업 후 `05_Logs/log.md`에 항목을 추가함. 양식과 작업유형 접두사 정의는 [[CONVENTIONS#7. 로그 항목 양식]] 참조.

- 작업이 끝날 때마다 반드시 기록 — 생략 금지
- log.md가 없으면 먼저 생성

## 바. 수정 금지 영역 준수

[[CONVENTIONS#6. 수정 금지 영역]]에 정의된 read-only 영역(`_locked/`, `_sources/`, `_figures/`, `_reviews/`)은 자율 편집 금지. 수정이 필요하다고 판단되면 사용자에게 먼저 확인하고 승인받은 후에만 작업함.

## 사. PDF 읽기 원칙

PDF 자료(RFP, 보고서, 논문 등)를 분석할 때는 다음 순서로 처리함.

1. **텍스트 추출 우선** — 텍스트 레이어가 있는 PDF는 `pdftotext -layout`로 추출하여 표/들여쓰기/계층 목차를 보존한 텍스트 파일로 변환 후 읽음
2. **스캔본은 이미지 변환** — 텍스트 추출이 비거나 깨지면 스캔본으로 판단, `pdftoppm -png`로 페이지별 PNG로 변환 후 멀티모달로 읽음
3. **Read 도구 직접 열기는 최후 수단** — 20페이지 제한, 표 정확도 저하, 토큰 비용 문제 때문에 위 두 방법이 불가능할 때만 사용

환경 의존적인 실행 경로(poppler 바이너리 절대경로 등)는 사용자 로컬 메모리에 저장되어 있음.

# 3. 검수 기준과 호출 방식

검수 항목은 P1~P5 5단계 우선순위로 분류함. 정의 정본은 [[codex 검수 포커스]]의 표준 포커스 텍스트. 본 절은 발췌 종류별 분담과 호출 형식만 명시하며, 포커스 텍스트 본문은 중복 게재하지 않음.

## 가. P1~P5 우선순위 (요약)

| 우선순위 | 검수 대상 | 정본 |
|---|---|---|
| P1 사실 오류/출처 미스매치 | 수치, 일정, 인용이 원문과 일치하는가. 출처 wikilink 동반. 깨진 링크 | [[codex 검수 포커스#2. 표준 포커스 텍스트\|P1 블록]] |
| P2 논리적 비약/추론 정정 | _locked 추론, 합계/누적 비약, 문서 간 모순 | [[codex 검수 포커스#2. 표준 포커스 텍스트\|P2 블록]] |
| P3 누락된 반론/견고성 | 검증 없이 "확정" 표기된 추정, 한계 조건 누락 | [[codex 검수 포커스#2. 표준 포커스 텍스트\|P3 블록]] |
| P4 톤/구조 일관성 | 음슴체, 가운뎃점, 두괄식/개조식, frontmatter, log 양식 | [[codex 검수 포커스#2. 표준 포커스 텍스트\|P4 블록]] |
| P5 위키 정합성 | MOC 등록, 양방향 wikilink, 깨진 링크 | [[codex 검수 포커스#2. 표준 포커스 텍스트\|P5 블록]] |

## 나. 발췌 종류별 가이드 (사실/정합 vs 적대)

| 발췌 종류 | 첨부 블록 | 시선 | 분담 |
|---|---|---|---|
| **사실/정합 발췌** | 머리말 + P1 + P5 + 끝맺음 | 출처 일치, 깨진 링크, 양방향 연결, MOC 등록 | 외부 검토자 (Codex 호출). 다만 wikilink anchor 전수 점검은 작성자 grep 패스가 효과적 |
| **적대 발췌** | 머리말 + P2 + P3 + 끝맺음 + [[반복 결함 카탈로그]] | 논리 비약, 누락 반론, 한계 조건. 회의 기조 강화 | 외부 검토자 (Codex 호출). fresh context 자동 보장으로 작성자 컨텍스트 격리 |

**P4 톤/구조 일관성**: 작성자의 자기검수 + 본문 반영 시 일괄 점검. 외부 검토자 호출에서 발췌 블록으로 별도 첨부하지 않음 (필요 시 작성자가 직접 grep으로 점검).

**P 분류는 발췌 블록 구분 목적**: 5역할 시절의 "역할별 분담"이 아닌 발췌 종류별 구분. 외부 검토자는 단일 역할이며 발췌 종류로 시선 조정.

## 다. 외부 검토자 호출

```
/codex:adversarial-review --background <발췌>
```

- `/codex:review`는 native 검수 전용 — focus text를 코드 레벨에서 차단(`codex-companion.mjs:268` `validateNativeReviewRequest`)함. focus 첨부가 필요한 외부 검토는 `/codex:adversarial-review` 사용
- 발췌 시 [[codex 검수 포커스]]의 머리말(회의 기조)을 함께 첨부함
- 단일 정본은 [[codex 검수 포커스]]. 본 AGENTS.md에 포커스 텍스트 본문을 중복 게재하지 않음
- Codex sandbox 권한 차단으로 wikilink anchor 전수 점검 등 grep 영역을 못 잡는 경우, 작성자가 Claude Code 자체 grep 패스로 보완 (위키 정합성 영역에서 특히 유효)

## 라. 보고 처리

- 외부 검토 보고는 사용자 G2에서 항목별 수용/보류/반려 결정 (§2.라 사용자 승인 게이트)
- 결정 결과는 작성자가 `review` 작업유형 커밋으로 반영
- 신규 결함 유형이 반복 발견되면 [[codex 검수 포커스]] 또는 [[반복 결함 카탈로그]]에 점검 항목을 추가 (점진 보강)

# 4. 하지 말 것

- 사용자가 요청하지 않은 문서를 임의로 삭제하지 않는다
- 확실하지 않은 정보를 사실처럼 적지 않는다 — 불확실하면 명시 ([[CONVENTIONS#바. 수치/목표 인용 원칙 (엄격)]] 준수)
- log.md 기록을 생략하지 않는다
- `_xxx/` 접두 폴더 하위 문서를 임의로 수정하지 않는다 ([[CONVENTIONS#6. 수정 금지 영역]] 정의)
- Codex 검수 결과를 사용자 확인 없이 자동 반영하지 않는다
- 외부 검토 발견을 G2 사용자 승인 없이 본문에 반영하지 않는다 — 작성자는 "수용" 표시 항목만 손댐
- 외부 근거 권위 등급(L1~L5, [[CONVENTIONS#사. 외부 근거의 권위 계층]])을 무시하고 L5 추론을 L1 정본처럼 단정 인용하지 않는다
