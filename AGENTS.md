---
title: 에이전트 작업 지침
type: reference
status: stable
tags: [meta, agent]
created: 2026-05-08
updated: 2026-08-21
---

@CONVENTIONS.md

> `@import`는 Claude Code 전용 기능임. Codex 등 import를 자동으로 펼치지 않는 도구로 본 문서를 읽을 때는 [[CONVENTIONS|위키 작성 규칙]]을 함께 읽을 것.

AI 에이전트(Claude Code, Codex)가 다각도 교차검증 워크플로(3역할: 작성자/외부 검토자/중재자)로 마크다운 문서를 작성/편집/연결/검수하는 운영 규칙. 위키 작성 자체의 표준(디렉토리 구조, 문체, 인용 원칙, 로그 양식 등)은 [[CONVENTIONS|위키 작성 규칙]]에서 정의: 본 문서가 자동 import 함.

# 1. 핵심 원칙

**에이전트가 위키 운영자**: 사용자가 주제를 던지면 3역할(작성자, 외부 검토자, 중재자)이 다각도 교차검증으로 작성/검수하고 사용자가 중재함. 자세한 분담은 [[#2.1 역할 분담]] 참조. 위키 작성 자체의 원칙(Atomic Notes, Wikilinks, 점진적 축적, 한국어 우선)은 [[CONVENTIONS#1. 핵심 원칙]] 참조.

**3역할 설계의 정신**: 실제 도구는 2개(Claude Code, Codex). 도구와 1:1 매핑하는 3역할: 작성자(Claude Code) / 외부 검토자(Codex) / 중재자(사용자).

# 2. 에이전트 작업 지침

## 2.1 역할 분담

| 역할 | 도구 | 책임 | 호출 |
|---|---|---|---|
| 작성자 (Writer) | Claude Code | 초안 작성/갱신 + 자기검수(P1/P5 인라인) + 사용자 승인 항목 본문 반영. wikilink/MOC/log 기록 ([[CONVENTIONS#5. 문서 작성/갱신 절차]] 준수) | 기본 대화 |
| 외부 검토자 (External Reviewer) | Codex | 발췌 첨부 호출로 외부 시선 확보. 사실/정합(P1/P5), 적대(P2/P3), 가독성 발췌 중 선택 | `/codex:adversarial-review` + 발췌 종류 첨부 |
| 중재자 (Arbiter) | 사용자 | 모든 검수 발견에 대한 수용/보류/반려 결정. 자동 반영 금지 | 대화 |

**설계 원리**

- **작성자 단일화**: 초안 작성 + 자기검수 + 사용자 승인 후 본문 반영을 같은 Claude Code 안의 단일 흐름으로. 작업 단계는 git history와 log 작업유형(`create`/`update`/`review`)으로 추적
- **외부 검토자 단일화**: Codex 단일 도구. 시선 분리는 발췌 종류로 구현 ([[#3.2 발췌 종류별 가이드 (사실/정합, 적대, 가독성)]]). `/codex:review`는 native 전용으로 focus text 차단: `/codex:adversarial-review`만 사용
- **외부 검토자는 fresh context 자동**: Codex 호출마다 새 thread 생성. 작성자 컨텍스트 격리 보장됨

## 2.2 역할별 호출 절차

### 2.2.1 작성자

- **진입 조건**: 사용자가 작업을 지시하거나 직전 사이클이 사용자 승인으로 종료됨
- **호출**: 기본 대화
- **4단계 흐름**:
	1. **구조 설계 → 초안 작성/갱신**: 본문 작성 전 **구조 먼저 설계**: ① 엔티티와 그 종류(모듈/프로세스, 자산/기능, 개요/상세 등) ② 위계와 순서(개요 → 상세, 상위 → 하위) ③ 각 항목이 올바른 종류/위치인가 ④ 형제 항목이 실제로 구별되는가 ⑤ [[CONVENTIONS#3.8 구조 우선 작성 (정보 설계)]]의 **작성 전 절차 적용**: 의미 단위로 분류하고 목차를 먼저 설계한 뒤 채움 (단순 문장 나열 금지, 4줄 이상이나 항목 3개 이상은 2단계 sub-bullet로, 같은 수준은 병렬 구조로). 작성 단계에서 적용하는 것이지 사후 점검만이 아님. (상세는 아래 셀프체크 구조 항목 + [[반복 결함 카탈로그]] STRUCT/CONTENT). 그 뒤 본문 .md 변경 + log 항목(`create`/`update`) + git commit
	2. **자동 정비와 lint**: 세 스크립트를 차례로 실행함. 면제 안 됨: **모든 사이클에서 무조건 실행**
		- `node scripts/wiki_number.mjs --write`: 헤딩 번호를 [[CONVENTIONS#4.2 헤딩 넘버링]] 규칙대로 다시 매기고, 번호가 바뀐 헤딩을 가리키는 anchor를 저장소 전체에서 함께 고침. **헤딩을 쓸 때 번호를 손으로 붙이지 않음**: 번호 없이 제목만 쓰고 이 스크립트에 맡김. 같은 문서에 같은 헤딩 텍스트가 둘 이상이면 anchor 자동 갱신이 보류되고 경고가 뜨므로, 그때는 사람이 확정함
		- `node scripts/wiki_lint.mjs`: 깨진 wikilink, anchor 정합, 가운뎃점, em dash, 폐기된 절 기호 표기, 폐기 어휘(`scripts/deprecated_terms.txt` 등록분, 백틱 언급은 면제, 2026-08-21 신설), MOC 미등록, 링크 하한을 점검함. error 0건 확인 (1건 이상이면 정정 후 재실행). MOC 등록과 링크 하한은 셀프체크에 있던 것을 2026-08-20에 여기로 옮겼고, em dash는 규칙만 있고 검사가 없던 것을 같은 날 넣었음
		- `node scripts/quote_check.mjs`: 원전 전사본을 행 번호로 인용한 대목을 원문과 문자 단위로 대조함. 발견 0건 확인. 셀프체크 `[인용 재대조]`를 사람 눈이 아니라 기계가 하도록 옮긴 것임 (2026-08-20 신설). 따옴표 인용이 없어도 행 번호 참조가 전사본 범위를 벗어나면 잡음 (2026-08-21 확장)
		- 커밋 시점에는 `scripts/hooks/pre-commit`이 위 세 점검을 다시 돌려 어긋나면 커밋을 막음 (설치: `git config core.hooksPath scripts/hooks`). 훅은 워킹트리가 아니라 **스테이징된 내용**을 검사하므로, 워킹트리에서 고쳤어도 `git add`를 안 하면 계속 막힘 (2026-08-20 변경). 훅은 변경된 .md의 frontmatter `updated`가 오늘 날짜인지도 검사함 (2026-08-21 이관: 셀프체크 자기보고 항목을 기계로 옮김). 커밋하면 `scripts/diff_companion.mjs`가 본문이 바뀌었는데 절 제목과 TL;DR이 그대로인 절을 알림으로 출력함(알림만 하고 커밋은 그대로 진행됨, 2026-08-21 MECH-4). 알림이 짚은 절은 파생 텍스트(절 제목, 라벨, 표 셀, MOC 요약, [[CONVENTIONS#5.2 문서 갱신 시]] 4번)가 여전히 맞는지 확인하고, 어긋난 것은 후속 커밋으로 고침
	3. **자기검수 (P1/P5 인라인 셀프체크)**: 작성 직후 같은 패스에서 진행함. 발견 항목은 채팅에 인라인으로 보고함. 정본으로 남을 수치와 목록은 채팅에만 적지 말고 파일에 쓴 뒤 채팅에서는 그 파일을 가리킴(채팅에 적은 수치는 자동 검사가 못 봄). wikilink anchor는 가정하지 말고 무조건 grep으로 검증함. **자기검수 중 발견된 작은 결함은 즉시 정정 가능** (별도 게이트 없이). 단정적 정정과 정책 변경은 G1에서 사용자가 결정함. **형식 점검(lint, 라벨, 구조)은 뜻이 비거나 어색한 문장을 못 잡음.** 그 자리는 작성자가 메울 수 없음: 작성자는 자기 의도를 이미 알아 글자 그대로 읽지 못하고, 주어와 서술어가 안 맞는 비문이나 모호한 용어를 매번 놓침. 그래서 **새로 쓰거나 고친 정본 문장은 크기 불문 맥락 독립 검토를 1회 거침**: 작은 변경은 깨끗한 컨텍스트의 새 서브에이전트가 검토하고(의도와 추론은 주지 않고, 완성 텍스트와 "문법, 명료성, 중복, 구조 결함을 찾아라"는 지시만 전달), 큰 변경이나 외부 노출 정본은 Codex가 가독성 발췌로 검토함. 사실 층 변경은 **사실 대조 검토**를 한 차례 더 거침: 검토자에게 전사본 경로만 주고 "본문의 각 사실 주장에 대해 근거 행 번호를 원문에서 찾아 제시하고, 못 찾으면 '없음'이라 답하라"는 과제를 줌(근거를 못 찾은 주장이 목록에 그대로 드러나는 과제 형식, 2026-08-21 신설). **독립 검토도 의미 판정이라 무발견(검토자가 결함을 찾지 못함)이 결함 부재의 증명이 되지 못하므로, 작성자는 "검증 완료" 같은 단정 대신 수행 사실과 결과 건수만 보고함("독립 검토 1회, 발견 N건, 반영 M건"). 이는 보고 어휘의 규칙일 뿐, 독립 검토를 거칠 의무는 앞 문장대로 유지됨** ([[codex 검수 포커스#3.3 가독성 발췌 (P4 + 명료성, 낯선 독자 시선)]], [[CONVENTIONS#3.4.2 쉽게 쓰기]])
	4. **사용자 승인 후 반영**: G2에서 사용자가 "수용"으로 표시한 외부 검토 발견 항목을 본문 반영. log 항목(`review`) + git commit
- **준수**: [[CONVENTIONS#5. 문서 작성/갱신 절차]] (디렉토리, 프론트매터, wikilink, MOC 등록 등)
- **셀프체크 체크리스트 (P1/P5 압축판)**:
	- 자동 lint 통과 (0건) 확인했는가
	- 모든 정량 주장에 출처 wikilink가 동반되는가 ([[CONVENTIONS#3.6 수치/목표 인용 원칙 (엄격)]] 준수)
	- **[인용 재대조]** 인용을 쓰는 **그 자리에서 원문을 다시 열었는가.** 앞서 읽었다는 기억으로 쓰지 않음. 원문을 옆에 두고도 옮겨 적는 사이에 어미가 바뀌고 주체가 뒤바뀌고 없던 말이 붙음 (2026-08-19 한 세션에서 인용 오류 17건). 여섯 가지를 인용마다 확인함. **행 번호를 붙인 인용은 `node scripts/quote_check.mjs`가 기계로 대조하므로 사람은 그 결과를 확인하고, 스크립트가 못 보는 것(따옴표 없이 풀어 쓴 요약 인용, 지문의 해석)만 눈으로 봄**
		- **따옴표 안은 문자 단위로 같은가**: 줄이거나 어미를 바꾸거나 조사를 손댔으면 따옴표를 벗기고 풀어 씀. 원문 표기와 다르게 적을 때는 원문을 괄호로 병기함 ([[CONVENTIONS#3.6 수치/목표 인용 원칙 (엄격)]])
		- **행 번호가 그 문구의 행인가**: 옆 행 대사를 끌어와 붙이지 않음
		- **지문의 주체와 인과를 바꾸지 않았는가**: 누가 그 동작을 하는지, 무엇 때문에 그러는지를 원문 그대로 둠
		- **원문에 없는 말을 더하지 않았는가**: "덤볐다", "되뇌는" 같은 서술을 지문에 없이 붙이지 않음
		- **부분을 전체로 만들지 않았는가**: 일부만 겹치는데 "똑같이", 일부 사례뿐인데 "마다", 한 번뿐인데 "되풀이"로 쓰지 않음
		- **인용한 행의 앞뒤를 통으로 읽었는가**: 낱줄만 뽑으면 다음 행에서 뒤집히는 것을 못 봄 (618~620행만 보면 화해로 끝나 보이나 622행이 뒤집음)
	- 추정/어림값에 "추정" 또는 "확인 필요" 표기가 있는가
	- frontmatter에 `type`과 `status`가 있는가 ([[CONVENTIONS#3.2 프론트매터 (YAML)]])
	- 음슴체, 가운뎃점 금지, 두괄식/개조식 ([[CONVENTIONS#3.4.1 기본 문체]]) 준수하는가
	- 쉬운 말을 쓰고 약어는 첫 등장 시 본디 용어를 병기했는가 ([[CONVENTIONS#3.4.2 쉽게 쓰기]])
	- 헤딩/넘버링 규칙 ([[CONVENTIONS#4. 헤딩 / 넘버링]]) 준수하는가
	- **[정보 설계]** 단순 문장 나열이 아니라 의미 단위로 분류, 항목화됐는가. 라벨에 4줄 이상이나 항목 3개 이상을 1단계 불릿로 욱여넣지 않고 2단계(라벨 부모 + sub-bullet)로 분리했는가 ([[CONVENTIONS#3.8 구조 우선 작성 (정보 설계)]])
	- **[정보 설계]** 같은 수준 항목이 같은 문법 형태(병렬 구조)인가. 헤더와 라벨만 훑어도 전체 흐름이 보이는가
	- **[전수]** 발견한 결함을 그 인스턴스만 고치지 말고 같은 결함류를 전수 점검했는가. 부분 정정 후 "완료" 선언 금지 ([[반복 결함 카탈로그]] PROCESS-001에 점검 항목)
- **완료 조건**: 작성 단계는 commit + 자동 lint 통과 + MOC/wikilink 확인. 반영 단계는 사용자 승인 항목 반영 후 commit

### 2.2.2 외부 검토자

- **진입 조건**: G1 통과 후 (작은 변경은 사이클 면제 [[#2.4.3 작은 변경 사이클 면제 (엄격)]] 적용 가능)
- **호출**: `/codex:adversarial-review --background <발췌>` + [[codex 검수 포커스]]의 발췌 첨부
- **발췌 종류 (시선 분리)**:
	- **사실/정합 발췌**: 머리말(회의 기조) + P1(사실 오류/출처) + P5(위키 정합성) + 끝맺음
	- **적대 발췌**: 머리말 + P2(논리 비약) + P3(누락 반론/한계 조건) + 끝맺음 + [[반복 결함 카탈로그]] 본문
- **두 종류 호출은 병렬 또는 직렬**: 같은 commit 대상에 둘 다 호출 가능 (큰 변경 시 권장). 작은 변경은 한 종류 또는 생략
- **권한 sandbox 한계 보완**: Codex가 grep/Get-ChildItem 등 권한 차단으로 wikilink anchor 전수 점검을 못 하는 경우, 작성자가 Claude Code 자체 grep 패스(B 패스)로 보완. 위키 정합성 영역(P5)은 작성자 자기검수 + B 패스 조합이 외부 시선보다 효과적인 경우 많음
- **산출**: 검수 보고 (발견 항목 리스트). 보고는 의미 판단에 기반한 2차 의견이지 확정 판정이 아님: 보고에 verdict(판정)나 approve(승인) 표기가 있어도 참고 정보로 취급하고, 수용 여부는 G2에서 사용자가 정함. Codex에 보내는 발췌의 회의 기조와 판정 어휘는 검토자의 관대화를 막는 장치이므로 그대로 둠([[codex 검수 포커스]])
- **완료 조건**: 보고 수신 후 G2 진입

### 2.2.3 중재자

- **진입 조건**: 자기검수 발견 또는 외부 검토 발견 도착 후
- **역할**: 항목별 수용/보류/반려 결정
- **권한**: 자동 반영 금지. 사용자 명시 승인 없이 본문 반영 안 됨
- **근거 평가**: 발견 항목이 "근거 약함" 플래그를 포함하면 L1~L5 권위 등급([[CONVENTIONS#3.7 외부 근거의 권위 계층]]) 중 어느 등급의 근거가 있는지 명시한 뒤 결정

## 2.3 작업 사이클

```
작성자: 초안 작성/갱신 → 자기검수(P1/P5 인라인) → log(create/update)/commit
   ↓
G1 사용자 검토 (발견 항목별 결정 + 외부 검토 진행 여부. 자동 통과 조건 충족 시 생략)
   ↓ (외부 검토 필요 결정 시)
외부 검토자: /codex:adversarial-review + 사실/정합 발췌 또는 적대 발췌 (병렬 또는 직렬, 둘 다 가능)
   ↓
G2 사용자 항목별 수용/보류/반려 (모든 반영은 명시 승인 필수)
   ↓
작성자: 수용 항목 본문 반영 → log(review)/commit → 다음 사이클
```

**사이클 정신**: 작성자 1회 호출 안에서 작성 + 자기검수 + (사용자 승인 후) 반영. 외부 검토는 큰 변경에만 선택적으로 호출.

## 2.4 사용자 승인 게이트

다각도 검증의 모든 본문 반영은 사용자 명시 승인 후. 자동 반영 금지.

### 2.4.1 G1: 작성자 자기검수 후

- **입력**: 작성자가 보고한 자기검수 발견 사항 (채팅 인라인 리스트)과 자동 lint 결과. 없으면 없다고 보고함
- **사용자 직접 검토**: G1의 핵심: 사용자가 작성자 보고를 보고 **추가 결함을 적극 탐색**. 작성자 사각지대(자기 작업 관대성, 위계/구조 모순, 의미적 결함)는 사용자가 가장 잘 잡음 (이번 운영 실증). 사용자 발견 패턴은 [[반복 결함 카탈로그]]에 누적해 작성자 셀프체크에 점진 흡수
- **사용자 결정**: 두 가지 분기 결합:
	- **결함 항목별 결정** (자기검수 + 사용자 직접 검토 발견 모두): 항목별 "즉시 정정" / "외부 검토에 넘김" / "반려"
	- **외부 검토 진행 여부**: "외부 검토 진행" / "외부 검토 생략, 사이클 종료" (작은 변경 면제 [[#2.4.3 작은 변경 사이클 면제 (엄격)]] 또는 사용자 판단)
- **반영 경로**: 자기검수 중 작성자가 이미 즉시 정정한 항목은 보고에 명시 (G1은 그 정정에 대한 사후 확인 + 사용자 추가 검토 + 외부 검토 진행 여부 결정)
- **자동 통과 조건**: 자동 lint 0건 + 작성자 자기검수 무발견 보고 + 작은 변경 면제 조건([[#2.4.3 작은 변경 사이클 면제 (엄격)]]) 충족 시 G1과 외부 검토를 생략하고 사이클을 종료함. 통과의 근거는 면제 조건(변경이 작고 면제 대상 종류에 해당함)이지 무발견 보고가 아님. 자기검수는 의미 판정이라 무발견은 결함 부재의 증명이 아니라 "못 찾았다"는 보고이며, 자동 통과의 필요조건일 뿐임. 사용자가 뒤늦게 결함을 발견하면 그 시점에 새 사이클로 정정함

### 2.4.2 G2: 외부 검토 후

- **입력**: 외부 검토자 보고서의 발견 항목 (표 형태 권장)
- **사용자 결정**: 항목별
	- **수용** → 작성자가 본문 반영 (log `review` 작업유형)
	- **보류** → log에 잔류 메모로 기록, 다음 사이클에서 재논의
	- **반려** → 기각 사유를 log에 명시
- **반영 권한**: 작성자는 사용자가 "수용"으로 표시한 항목만 손댐. 다른 항목 무단 반영 금지

### 2.4.3 작은 변경 사이클 면제 (엄격)

면제 대상은 **외부 검토(Codex 호출)만**. 자동 lint + 자기검수 + 사용자 G1은 항상 수행.

면제 가능 조건 (모두 명백히 작은 변경):

- frontmatter only 수정 (`updated` 갱신 등)
- 1줄 단일 정정 (오타 또는 명백한 stale 수정 1건)
- 운영 메타 변경 (역할 분담/사이클 규칙 자체 변경: 외부 검토자가 검수하기 부적합)

**면제 불가**: 새 절/단락 신설, 표 구조 변경, 다수 라인 정정, 본문 정책 변경, 새 문서 생성, 헤딩 시프트: 위 조건에 부합하지 않으면 외부 검토 진행 또는 사용자 명시 면제 결정 필요.

면제 적용 시 log 항목에 `(cycle-exempt)` 표기 + 면제 사유 명시.

## 2.5 로그 기록 의무

모든 작업 후 `08_Logs/log.md`에 항목을 추가함. 양식과 작업유형 접두사 정의는 [[CONVENTIONS#7. 로그 항목 양식]] 참조.

- 작업이 끝날 때마다 반드시 기록: 생략 금지
- log.md가 없으면 먼저 생성

## 2.6 수정 금지 영역 준수

[[CONVENTIONS#6. 수정 금지 영역]]에 정의된 read-only 영역(`_locked/`, `_sources/`, `_figures/`, `_reviews/`)은 자율 편집 금지. 수정이 필요하다고 판단되면 사용자에게 먼저 확인하고 승인받은 후에만 작업함.

## 2.7 PDF 읽기 원칙

PDF 자료(RFP, 보고서, 논문 등)를 분석하는 절차는 [[PDF 읽기 절차]]가 정본임 (2026-08-21 이관, 상시 로드 감량). 순서 요약: 텍스트 추출 우선, 스캔본은 이미지 변환, Read 도구 직접 열기는 최후 수단.

## 2.8 위키 운영 워크플로 (수집/질의/유지)

[[CONVENTIONS#1. 핵심 원칙]]의 "수집 시점 컴파일, 복리 축적"을 작성자가 실제로 수행하는 세 동작(수집 Ingest, 질의 Query, 유지 Maintenance)임. 절차 정본은 [[위키 운영 워크플로]]임 (2026-08-21 이관, 상시 로드 감량). 세 동작 모두 작성자([[#2.2.1 작성자]]) 동작이며, 본문 변경이 생기면 다른 작성 작업과 똑같이 G1(필요 시 G2) 게이트를 거침. log 작업유형은 수집 `ingest`, 질의 `query`, 유지는 건강성 점검 `lint`와 구조 정비 `maint`임.

## 2.9 산출물층 개입 절차 (05_산출물)

`05_산출물/`은 **사용자가 직접 쓰는 원고**임. 콘텐츠 기획서 6종과 킥오프 발표자료가 있음. 위키 작성 규칙을 적용하지 않음.

**시작과 끝만 에이전트가 맡고 가운데는 사용자가 씀** (2026-08-19 사용자 결정).

1. **초안**: 사용자가 부르면 에이전트가 초안을 씀
2. **집필**: 사용자가 직접 씀. **에이전트는 손대지 않음**
3. **검수**: 사용자가 부르면 에이전트가 검수함. 발견은 보고만 하고 반영은 G2 승인 뒤에 함 ([[#2.4.2 G2: 외부 검토 후]])

**자율 편집 금지**: `_locked/`처럼 읽기 전용은 아니지만, 사용자 호출 없이 편집하지 않음. 다른 작업을 하다 이 폴더의 결함을 발견하면 고치지 말고 보고함. 링크가 깨지는 기계적 정비(문서 이름이 바뀌어 anchor를 따라 고치는 등)는 예외로 하되 log에 남김.

**검수할 때 보는 것**: 위키 규칙 위반은 결함이 아님. 본문 위키링크와 임베드가 없는지(자족성), 정본과 수치가 맞는지([[CONVENTIONS#3.6 수치/목표 인용 원칙 (엄격)]]), 논리와 가독성이 성립하는지를 봄.

# 3. 검수 기준과 호출 방식

검수 항목은 P1~P5 5단계 우선순위로 분류함. 정의 정본은 [[codex 검수 포커스]]의 표준 포커스 텍스트. 본 절은 발췌 종류별 분담과 호출 형식만 명시하며, 포커스 텍스트 본문은 중복 게재하지 않음.

## 3.1 P1~P5 우선순위 (요약)

| 우선순위 | 검수 대상 | 정본 |
|---|---|---|
| P1 사실 오류/출처 미스매치 | 수치, 일정, 인용이 원문과 일치하는가. 출처 wikilink 동반. 깨진 링크 | [[codex 검수 포커스#2. 표준 포커스 텍스트\|P1 블록]] |
| P2 논리적 비약/추론 정정 | _locked 추론, 합계/누적 비약, 문서 간 모순 | [[codex 검수 포커스#2. 표준 포커스 텍스트\|P2 블록]] |
| P3 누락된 반론/견고성 | 검증 없이 "확정" 표기된 추정, 한계 조건 누락 | [[codex 검수 포커스#2. 표준 포커스 텍스트\|P3 블록]] |
| P4 톤/구조 일관성 | 음슴체, 가운뎃점, 두괄식/개조식, frontmatter, log 양식 | [[codex 검수 포커스#2. 표준 포커스 텍스트\|P4 블록]] |
| P5 위키 정합성 | MOC 등록, 양방향 wikilink, 깨진 링크 | [[codex 검수 포커스#2. 표준 포커스 텍스트\|P5 블록]] |

## 3.2 발췌 종류별 가이드 (사실/정합, 적대, 가독성)

| 발췌 종류 | 첨부 블록 | 시선 | 분담 |
|---|---|---|---|
| **사실/정합 발췌** | 머리말 + P1 + P5 + 끝맺음 | 출처 일치, 깨진 링크, 양방향 연결, MOC 등록 | 외부 검토자 (Codex 호출). 다만 wikilink anchor 전수 점검은 작성자 grep 패스가 효과적 |
| **적대 발췌** | 머리말 + P2 + P3 + 끝맺음 + [[반복 결함 카탈로그]] | 논리 비약, 누락 반론, 한계 조건. 회의 기조 강화 | 외부 검토자 (Codex 호출). fresh context 자동 보장으로 작성자 컨텍스트 격리 |
| **가독성 발췌** | 머리말 + 가독성/명료성 블록 + 끝맺음 | 모호 용어, 구어체, 조어, 반복, 단순 나열, 비병렬 구조 (낯선 독자 시선) | 외부 검토자 (Codex 호출). 서브에이전트 독립 검토가 못 본 가독성 결함의 외부 안전망 |

**P4 톤/구조 + 가독성**: 핵심은 맥락 독립임. 맥락 안에서 하는 자기검수는 작성자가 자기 의도를 이미 알아 효과가 약하므로 단독으로 신뢰하지 않음. 새로 쓰거나 고친 정본 문장은 크기 불문 맥락 독립 검토를 1회 거침(절차는 [[#2.2.1 작성자]]). 작은 변경은 새 서브에이전트가 검토하고, 큰 변경이나 외부 노출 정본은 Codex가 **가독성 발췌**로 검토함 ([[codex 검수 포커스#3.3 가독성 발췌 (P4 + 명료성, 낯선 독자 시선)]]). 작은 변경이라도 가독성과 문법 검토를 생략하지 않음.

**P 분류는 발췌 블록 구분 목적**: 5역할 시절의 "역할별 분담"이 아닌 발췌 종류별 구분. 외부 검토자는 단일 역할이며 발췌 종류로 시선 조정.

## 3.3 외부 검토자 호출

```
/codex:adversarial-review --background <발췌>
```

**호출 조건** (다음 중 하나라도 해당하면 호출):

- **큰 변경**: 새 절/단락 신설, 표 구조 변경, 다수 라인 정정, 본문 정책 변경, 새 문서 생성, 헤딩 시프트
- **외부 노출 산출물**: 기획서/결정사항/정본/제안서 변경
- **사용자가 "외부 시선 필요" 명시** 시
- **정량 인용 또는 사실 주장이 다수 포함된 작업** (수치/일정/출처)

**호출 면제**: [[#2.4.3 작은 변경 사이클 면제 (엄격)]]의 조건과 같음(그 절이 정본이라 여기 다시 적지 않음, 2026-08-21).

호출 조건에 부합하면 작성자가 G1 통과 후 자동 호출 제안. 사용자가 명시 거부 시 면제 (사유는 log에 기록).

**호출 운용 규칙**:

- `/codex:review`는 native 검수 전용: focus text를 코드 레벨에서 차단(`codex-companion.mjs:268` `validateNativeReviewRequest`)함. focus 첨부가 필요한 외부 검토는 `/codex:adversarial-review` 사용
- 발췌 시 [[codex 검수 포커스]]의 머리말(회의 기조)을 함께 첨부함
- 단일 정본은 [[codex 검수 포커스]]. 본 AGENTS.md에 포커스 텍스트 본문을 중복 게재하지 않음
- Codex sandbox 권한 한계를 자동 lint와 작성자 grep 패스로 보완하는 규칙은 [[#2.2.2 외부 검토자]]가 정본임 (같은 규칙을 두 곳에 적지 않음, 2026-08-21). Codex는 의미적 결함(P1 사실/P2 논리/P3 한계 조건)에 집중

## 3.4 보고 처리

- 외부 검토 보고는 사용자 G2에서 항목별 수용/보류/반려 결정 ([[#2.4 사용자 승인 게이트]])
- 결정 결과는 작성자가 `review` 작업유형 커밋으로 반영
- 신규 결함 유형이 반복 발견되면 [[codex 검수 포커스]] 또는 [[반복 결함 카탈로그]]에 점검 항목을 추가 (점진 보강)

# 4. 하지 말 것

- 사용자가 요청하지 않은 문서를 임의로 삭제하지 않는다
- 확실하지 않은 정보를 사실처럼 적지 않는다: 불확실하면 명시 ([[CONVENTIONS#3.6 수치/목표 인용 원칙 (엄격)]] 준수)
- log.md 기록을 생략하지 않는다
- `_xxx/` 접두 폴더 하위 문서를 임의로 수정하지 않는다 ([[CONVENTIONS#6. 수정 금지 영역]] 정의)
- 결과물(`04_Projects`, `05_산출물`)에 에이전트 작업 지침이나 TODO(표준 정합 잔여, 가독성 정비, 위키 메커니즘 메모 등)를 섞지 않는다: `07_작업노트/`에 콘텐츠별로 분리 ([[CONVENTIONS#2. 디렉토리 구조]])
- `05_산출물/`을 사용자 호출 없이 편집하지 않는다: 초안과 검수만 맡고 집필은 사용자 몫 ([[#2.9 산출물층 개입 절차 (05_산출물)]])
- Codex 검수 결과를 사용자 확인 없이 자동 반영하지 않는다
- 외부 검토 발견을 G2 사용자 승인 없이 본문에 반영하지 않는다: 작성자는 "수용" 표시 항목만 손댐
- 외부 근거 권위 등급(L1~L5, [[CONVENTIONS#3.7 외부 근거의 권위 계층]])을 무시하고 L5 추론을 L1 정본처럼 단정 인용하지 않는다
